# 작업 인수인계 — SHMS(안전보건관리체계 이행 관리 시스템)
> 이 파일은 다른 AI(GPT 등)에게 이어서 작업을 요청할 때 그대로 전달하기 위한 문서입니다.
> 작성 시각: 2026-08-13

---

## 1. 프로젝트 개요

- **위치**: `D:\■ AI 활용 안전전보건 자료\7. 안전보건관리체계 이행 관리 앱(중처법, 산안법 관리)`
- **목적**: 중대재해처벌법(MSSA) · 산업안전보건법(OSHA) · ISO 45001 요구사항의 이행 여부를 반기 단위로 기록·관리하는 웹앱
- **기술 스택**: 순수 ES Module(빌드 도구 없음), GitHub Pages 정적 배포, localStorage 저장(Supabase는 아직 미연결)
- **저장소**: GitHub `main` 브랜치 → GitHub Actions → GitHub Pages 자동 배포
- **현재 git 상태**: `main` 브랜치, 클린(clean), origin과 동기화됨
- **최근 커밋 (최신순)**:
  ```
  4c940c3 이행항목 카드 디자인 전면 개선: 관련문서·ISO 강조 영역 분리 + 정보 계층 재구성
  96be8fd 전역 스피너 오버레이 추가: 저장·로딩 시 회전 로딩 표시로 작업 진행 상태 알림
  b593d98 작업인수인계 2026-08-12 기록 추가: Notion 편집 기능 + 첨부파일 UI
  77a9891 Notion 데이터 편집 기능 + 첨부파일 등록
  be91400 로그인 비밀번호 표시/숨김 토글 버튼 추가
  ```
- **현재 캐시버스트(cache-bust) 버전**: `?v=20260812_ui`

---

## 2. 반드시 지켜야 할 규칙 (다음 작업자 필독)

1. **`js/data/frameworks.js`의 조항 `id` 값을 절대 변경하지 말 것** — id가 이행기록(PK)이므로 바꾸면 기존 기록이 유실됨
2. **캐시버스트 규칙** — `?v=` 값은 `index.html`과 `js/app.js`, `js/core.js`, `js/views-core.js`, `js/views-ext.js` 5개 파일의 import 구문에 **전부 동일한 값**으로 들어가야 함. 소스를 고치면 반드시 **새 값**으로 올려야 함(같은 값이면 브라우저가 직전 배포본을 계속 씀). 상세 규칙은 프로젝트 `CLAUDE.md` 4절 참조.
3. **수정 전 체크포인트 커밋** → **한 번에 하나만 수정** → **배포 전 변경 목록 보고**
4. 실명(개인정보) 소스에 포함 금지 — 법령/운영 정보만
5. 사용자 지시: "추후 배포 여부는 묻지 말고 진행" — 배포는 확인 없이 진행하되, 배포 후 실제 반영 여부는 검증할 것
6. 작업 완료 시 `작업인수인계.md` 하단에 정해진 형식으로 기록 추가 (프로젝트 `CLAUDE.md` 8절 형식 준수)

---

## 3. 파일별 역할

| 파일 | 역할 |
|---|---|
| `js/data/frameworks.js` | 법령·ISO·문서체계 마스터 데이터 (48개 이행항목 + 문서체계) |
| `js/core.js` | 유틸, 인증(공용 비밀번호 게이트), 데이터 저장 계층, 스피너 유틸 |
| `js/views-core.js` | 대시보드, 법령/ISO 이행항목 카드, 작성 드로어 |
| `js/views-ext.js` | 문서체계·점검·CAPA·증빙·조직·심사대응·설정 화면 |
| `js/app.js` | 셸, 라우팅, 로그인, 부팅 스피너 |
| `css/style.css` | 디자인 시스템 전체 |
| `supabase/schema.sql` | (미연결 상태) 테이블·RLS 정의 |

---

## 4. 이번 세션에서 완료한 작업

### (1) Notion 데이터 편집 기능 + 첨부파일 (이전 세션에서 시작, 이번 세션 초반 마무리)
- 이행항목 드로어에 3개 필드(작성및보관자료 / 준비현황 / 증빙) 수기 편집 기능 추가
- 첨부파일 등록 UI 추가 (`js/core.js`의 레코드 기본값에 `attachments: []` 추가)

### (2) 전역 스피너(로딩 표시) 추가
- **배경**: 사용자가 "작업 중인지 완료된 건지 알 수가 없다"고 지적
- `js/core.js`에 `showSpinner(msg)` / `hideSpinner()` 함수 추가 — `#spinnerOverlay` DOM을 만들어 회전 원(spinner-ring) 표시
- `css/style.css`에 `.spinner-overlay`, `.spinner-ring` 애니메이션 스타일 추가
- 적용 위치: `js/app.js`의 `boot()`(자료 불러오는 중), `js/views-core.js`의 드로어 저장, `js/views-ext.js`의 문서/점검/CAPA/증빙/조직 저장 전체

### (3) 이행항목 카드 디자인 전면 개선
- **배경**: 사용자가 "관련문서 등이 눈에 확 안 띈다", "디자인이 단조롭고 전문성이 안 느껴진다"고 지적하며 전면 개선 요청
- `js/views-core.js`의 `itemCard()` 함수를 5단 정보 계층으로 완전히 재작성:
  1. 헤더(조항 코드 배지 + 제목 + 그룹명 + 핵심의무/상태 배지)
  2. 이행률 게이지 바
  3. 본문 블록(법령 요구사항 / 이행 현황, 각각 라벨 표시)
  4. 팩트 그리드(점검주기·담당·최근점검·기한, 기한 초과 시 빨간색 경고)
  5. 하단 연계정보 스트립(관련문서 — 문서명까지 표시, ISO 조항, 벌칙, 자료보유 현황 칩)
- `css/style.css`에 `.item-rail`(그라데이션 상태 레일), `.item-head`, `.item-gauge`, `.item-body`, `.item-block`, `.item-facts`(그리드), `.item-links`(하단 스트립), 칩 시스템(`.chip-doc`, `.chip-iso`, `.chip-pen`, `.chip-cnt`) 전면 추가
- 상태별(이행완료/이행중/미이행/보완필요) 4종 미리보기 HTML을 만들어 사용자에게 전달 후 검증 완료
- 커밋 `96be8fd`, `4c940c3` 로 배포 완료

### (4) 데이터 완전성 점검 (분석만, 수정 아님)
Notion 원본 데이터 중 비어있는 필드 확인 결과:
| 필드 | 채워짐 | 비어있음 |
|---|---|---|
| 작성·보관자료 목록 | 39/48 | 9 |
| 당사 준비현황 | 40/48 | 8 |
| 증빙자료 목록 | 24/48 | **24** |

→ Notion 원본 자체가 공란이던 항목들이며, 이제 앱에서 수기 입력 가능하므로 사용자가 직접 채워야 함 (자동 채움 불가 — 실제 회사 자료이므로 임의 생성 금지)

---

## 5. 미완료 / 다음 작업자가 봐야 할 것

1. **`작업인수인계.md` 최신 기록 미반영** — 스피너 추가(96be8fd)와 카드 디자인 개선(4c940c3) 작업이 `작업인수인계.md`에 아직 기록되지 않음. 다음 작업 시 프로젝트 `CLAUDE.md` 8절 형식으로 추가 필요.
2. **증빙자료 24개 항목 공란** — 사용자가 직접 앱에서 입력해야 하는 실제 데이터 공백. AI가 임의로 채우면 안 됨.
3. **Supabase 미연결 상태 지속** — 현재 localStorage만 사용 중. Supabase 연결 시 `js/core.js`의 인증 방식(공용 비밀번호 게이트)과 `shms_can_edit()` RLS 정책이 충돌하는 미해결 과제가 있음(프로젝트 `CLAUDE.md` 6절 참조) — Supabase 연결 작업 시 인증 방식 재설계 필요.
4. **OSHA(산안법) 페이지에서 카드 신디자인 검증 미완료** — 벌칙(penalty) 필드가 카드에 정상 표시되는지 실제 브라우저에서 재확인 필요 (중처법 항목은 벌칙 필드 없음, 산안법 항목만 있음).
5. 사용자가 카드 디자인 개선 결과에 대해 아직 피드백을 주지 않음 — 추가 요청이 있을 수 있음.

---

## 6. 발생했던 오류와 해결

- PowerShell 문법을 Bash 도구로 실행 시도 → 오류. PowerShell 전용 도구로 전환하여 해결.
- 캐시버스트 버전을 올렸는데도 브라우저가 구 버전을 표시(hasRail: false) → `location.reload(true)`로 강제 새로고침하여 해결.
- Browser pane 스크린샷이 "화면이 표시되지 않아 프레임을 합성할 수 없음" 오류로 실패 → DOM/computed CSS를 JavaScript로 직접 조회하는 방식으로 우회.
- 한글 파일명(백슬래시 포함) 전송 시 JSON 파싱 오류 → 경로에 슬래시(`/`)로 변경하여 해결.

---

## 7. 현재 작업 상태: **보류(대기 중)**

카드 디자인 개선은 배포 완료 상태이며, 사용자 피드백 대기 중입니다. 긴급하게 이어서 처리해야 할 미완료 작업은 없으나, 위 5절의 항목들을 다음 세션에서 확인/진행하면 됩니다.
