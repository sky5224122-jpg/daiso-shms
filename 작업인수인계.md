# 작업 인수인계 — 안전보건관리체계 이행 관리 시스템 (SHMS)

### 2026-08-13 자동 백업을 매일 오전 9시 1회로 변경

- 수정한 파일:
  - `js/core.js`: 저장·삭제 때마다 실행하던 자동백업을 제거하고, 앱이 열린 상태에서 매일 오전 9시에 한 번 생성하는 예약 백업으로 변경함.
  - `js/app.js`: 자료 로드 후 매일 오전 9시 예약 백업을 시작하도록 연결함.
  - `js/views-ext.js`: 설정·백업 화면의 자동백업 설명을 실제 동작과 수동 백업 권장 방식에 맞게 변경함.
  - `index.html`, `js/app.js`, `js/core.js`, `js/views-core.js`, `js/views-ext.js`: 캐시버스트를 `20260813_dailybackup1`로 통일함.
  - `작업인수인계.md`: 이번 작업 결과를 기록함.
- 수정한 기능:
  - 업무 저장·수정·삭제가 발생해도 자동백업을 추가로 만들지 않음.
  - 앱이 열린 경우 다음 오전 9시에 하루 한 번만 브라우저 자동백업을 만들고, 최근 5개만 보관함.
  - 중요한 작업 직후 또는 앱·PC가 오전 9시에 꺼져 있는 경우에는 `전체 백업 내려받기`를 수동 실행하도록 화면에 안내함.
- 건드리지 않은 기능:
  - 전체 백업 JSON 내려받기·복원, 기존 자동백업 복원·삭제, 첨부 원본 저장, Supabase/R2 연결과 업무 데이터 구조.
- 다음 작업자가 이어서 봐야 할 내용:
  1. 브라우저 기반 예약은 앱이 열려 있을 때만 동작한다. PC가 꺼져 있거나 브라우저가 종료된 상태에서도 정시 백업이 필요하면 Supabase 또는 Cloudflare의 서버 스케줄러를 별도로 구축해야 함.
  2. 자동백업은 여전히 각 브라우저의 localStorage에만 보관되며, 공동 운영용 서버 백업은 아님.
- 테스트해야 할 항목:
  - 저장 후 자동백업 목록이 바로 늘지 않는지 확인.
  - 앱을 오전 9시 이전부터 열어 둔 상태에서 9시가 지난 뒤 자동백업 1개만 생성되는지 확인.
  - 수동 전체 백업 내려받기와 기존 자동백업 복원·삭제가 정상 동작하는지 확인.
- 실제 검증 결과:
  - `node --check`, `git diff --check`, `_tools/github_deploy.ps1 -CheckOnly` 통과.
  - 저장 시 자동백업 호출과 기존 1분 간격 안내 문구가 남아 있지 않은 것을 확인함.

### 2026-08-13 이행점검·중대재해처벌법 자동 연동 정리

- 수정한 파일:
  - `js/views-ext.js`: 이행점검 화면을 중대재해처벌법 상세기록의 자동 연동 현황과 점검 회차 총괄 기록으로 분리함.
  - `index.html`, `js/app.js`, `js/core.js`, `js/views-core.js`, `js/views-ext.js`: 캐시버스트를 `20260813_autolink1`로 통일함.
  - `작업인수인계.md`: 이번 작업 결과를 기록함.
- 수정한 기능:
  - 중대재해처벌법 상세창에서 입력한 이행현황·최근 점검일·담당·상태가 이행점검의 `중대재해처벌법 자동 연동 현황`에 별도 저장 없이 즉시 표시됨.
  - 기존에 중복되던 반기 점검 대상 체크리스트는 제거하고, 중대재해처벌법 13개 의무를 한 표에서 확인·클릭해 원본 상세기록으로 이동하도록 정리함.
  - 이행점검의 수기 입력은 법령별 이행내용을 다시 쓰는 용도가 아니라 전사·현장 점검 회차의 총괄 결과, 지적사항, 조치계획, 결과보고서·사진 첨부용으로 명확히 변경함.
  - 자동연동 건수와 미이행·보완 우선조치 건수를 상단 지표로 표시함.
- 건드리지 않은 기능:
  - 중대재해처벌법 원본 항목과 상세 입력값, 기존 점검 회차 기록·첨부, 작성·수정·삭제 권한, Supabase/R2 데이터 구조 및 저장 로직.
- 다음 작업자가 이어서 봐야 할 내용:
  1. 자동 연동은 동일 브라우저 또는 Supabase 연결 시 동일 원격 기록을 읽는 표시 연동이며, `shms_inspections`에 법령별 내용을 중복 복사하지 않음.
  2. 산업안전보건법·ISO까지 이행점검 자동 연동 범위를 확장하려면 사용자 요청에 따라 별도 화면·보고서 범위를 정해 진행함.
- 테스트해야 할 항목:
  - 중대재해처벌법 상세창에서 이행현황·점검일·담당·상태를 저장한 후 이행점검 화면에 즉시 동일하게 표시되는지 확인.
  - 자동 연동 표의 행을 클릭했을 때 해당 법령 상세창이 열리는지 확인.
  - 점검 회차 총괄 기록의 저장·첨부·재조회가 기존 자료와 충돌 없이 동작하는지 확인.
- 실제 검증 결과:
  - `node --check`, `git diff --check`, `_tools/github_deploy.ps1 -CheckOnly` 통과.
  - 로컬 브라우저에서 중대재해처벌법 상세의 이행내역·점검일·담당 저장 후 이행점검 자동 연동 표에 같은 값이 표시되는 것을 확인함.

### 2026-08-13 대표이사 보고용 종합 보고서 추가

- 수정한 파일:
  - `js/views-core.js`: 종합 대시보드의 현재 반기 데이터를 집계해 경영진 보고서를 생성하고, 미리보기·PDF 인쇄 이벤트를 추가함.
  - `js/app.js`: 대시보드 전용 이벤트를 연결함.
  - `css/style.css`: 대표이사 보고서 전용 화면·인쇄 레이아웃을 추가함.
  - `index.html`, `js/app.js`, `js/core.js`, `js/views-core.js`, `js/views-ext.js`: 캐시버스트를 `20260813_execreport1`로 통일함.
  - `작업인수인계.md`: 이번 작업 결과를 기록함.
- 수정한 기능:
  - 종합 대시보드에 `대표이사 보고용 안전보건관리체계 종합 보고서` 진입 영역과 `보고서 보기`, `PDF 인쇄 · 저장` 버튼을 추가함.
  - 보고서는 선택 반기 기준으로 통합 이행지수, 경영진 판단 신호, 중대재해처벌법·산업안전보건법·ISO 45001 이행 수준, 중대·기한임박 우선 과제, 대표이사 요청사항, 문서·반기점검·CAPA·증빙 운영지표를 자동 집계함.
  - 인쇄 시에는 보고서 본문만 정돈된 형태로 출력되어 브라우저의 PDF 저장 기능으로 보고서 파일을 만들 수 있음.
  - 법률 적합성의 최종 판단은 원본 증빙과 현장 확인이 필요하다는 기준 문구를 보고서에 명시함.
- 건드리지 않은 기능:
  - 법령·ISO 원본 데이터, 작성·수정·삭제 권한, 첨부·압축·저장 로직, Supabase/R2 연결 설정 및 데이터 구조.
- 다음 작업자가 이어서 봐야 할 내용:
  1. 현재 보고서는 시스템에 등록된 데이터로 자동 작성되므로, 대표이사 정식 보고 전 각 항목의 이행상태·기한·CAPA·증빙 최신화가 선행되어야 함.
  2. 보고서에 대표이사 성명·결재란·회사 로고·별도 표지 양식이 필요하면 별도 요청 범위로 추가함.
- 테스트해야 할 항목:
  - 반기 선택을 변경한 뒤 보고서의 기간·수치·우선과제가 함께 갱신되는지 확인.
  - 중대 미이행, CAPA 기한초과, 기한임박 항목을 등록했을 때 경영진 판단 신호와 대표이사 요청사항이 맞게 바뀌는지 확인.
  - `PDF 인쇄 · 저장`에서 미리보기와 PDF 페이지 분할이 사내 PC 브라우저에서 적절한지 확인.
- 실제 검증 결과:
  - `node --check`, `git diff --check`, `_tools/github_deploy.ps1 -CheckOnly` 통과.
  - 로컬 브라우저에서 `sky1234` 로그인 후 대시보드의 보고서 열기 → 보고서 제목·경영진 판단 신호·대표이사 요청사항·PDF 인쇄 버튼 표시까지 확인함.

### 2026-08-13 마스터 삭제 권한 재검증

- 수정한 파일:
  - `작업인수인계.md`: 재검증 결과만 기록했으며 앱 기능 코드는 변경하지 않음.
- 수정한 기능:
  - 없음. 기존 배포본의 2단계 비밀번호 및 마스터 삭제 권한을 실제 GitHub Pages에서 재확인함.
  - `sky1234` 로그인 시 사용자 표기가 `마스터 관리자 / 시스템 관리자`로 바뀌고, 법령 상세창의 `기록 삭제` 버튼이 노출되는 것을 확인함.
  - 일반 접속 비밀번호는 작성·수정·첨부만 가능하고, 코드상 `master-gate` 역할이 아니면 모든 삭제 함수가 즉시 차단됨.
- 건드리지 않은 기능:
  - 기존 공용 비밀번호, 작성·수정·첨부 흐름, 법령·ISO 기준 항목, Supabase/R2 연결 설정 및 데이터 구조.
- 다음 작업자가 이어서 봐야 할 내용:
  1. 현재 정적 웹앱의 비밀번호 권한은 화면·앱 로직 기준임. Supabase 공동 운영 전환 시에는 Supabase Auth와 RLS에 삭제 권한을 서버 측으로도 동일하게 적용해야 함.
  2. 기존 로그인 상태에서는 역할이 남아 있을 수 있으므로, 마스터 권한 확인은 로그아웃 후 `sky1234`로 다시 로그인해서 진행함.
- 테스트해야 할 항목:
  - 일반 비밀번호 로그인에서 문서·점검·CAPA·증빙·선임·법령 기록·첨부·자동백업의 삭제 버튼이 보이지 않고 삭제 호출도 거절되는지 확인.
  - `sky1234` 로그인에서 위 모든 삭제 버튼과 실제 삭제(확인창 포함)가 정상 동작하는지, 특히 선임자 등록 5개 항목이 삭제되는지 확인.
  - GitHub Pages 실배포에서 `sky1234` 로그인 → `마스터 관리자` 표기 → 법령 상세창 `기록 삭제` 버튼 노출까지 확인 완료.

### 2026-08-13 마스터 전용 삭제 권한 추가

- 수정한 파일:
  - `js/core.js`: 공용 작성 비밀번호와 마스터 관리자 비밀번호를 분리하고, 마스터 전용 `canDelete()` 및 행·법령기록·자동백업의 저장 계층 삭제 권한 검사 추가
  - `js/views-core.js`: 마스터에게만 법령·ISO 상세창의 `기록 삭제`와 저장 완료 첨부자료 삭제 노출
  - `js/views-ext.js`: 문서·점검·CAPA·증빙·선임·자동백업 삭제 버튼과 이벤트를 마스터 전용으로 변경
  - `index.html`, `js/app.js`, `js/core.js`, `js/views-core.js`, `js/views-ext.js`: 캐시버스트를 `20260813_masterdelete1`로 통일
  - `작업인수인계.md`: 이번 작업 결과 기록
- 수정한 기능:
  - 기존 공용 비밀번호 사용자는 작성·수정·새 첨부 추가는 가능하지만 기존 기록·첨부·백업을 삭제할 수 없다.
  - `sky1234`로 접속하면 마스터 관리자 역할이 부여되어 모든 업무 내역을 삭제할 수 있다.
  - 삭제 범위는 문서, 이행점검, CAPA, 증빙 자료, 조직·법정선임, 법령·ISO 항목의 작성 기록, 저장 완료 첨부자료, 자동 백업이다.
  - 선임자 등록의 경영책임자, 안전보건관리담당자, 산업보건의, 명예산업안전감독관, 근로자대표를 포함한 모든 등록 선임자는 마스터가 삭제할 수 있다.
  - 행 또는 법령기록 삭제 성공 시 연결된 첨부 원본도 함께 정리한다. 법령·ISO의 기준 항목 자체는 삭제하지 않고 해당 반기 작성 기록만 지운다.
- 건드리지 않은 기능:
  - 공용 비밀번호 값과 작성·수정 권한, 법령·ISO 마스터 항목, 첨부 추가·열람 기능, Supabase/R2 실제 설정
- 다음 작업자가 이어서 봐야 할 내용:
  1. 정식 Supabase Auth/RLS 전환 전까지는 브라우저 코드의 비밀번호 기반 권한이므로 완전한 서버 보안은 아니다. 원격 DB 정책에도 마스터 삭제 정책을 추가해야 한다.
  2. 마스터 비밀번호 해시는 `MASTER_GATE_HASH`에 보관되어 있다. 비밀번호 변경 시 SHA-256 해시로 교체한다.
- 테스트해야 할 항목:
  - 공용 비밀번호 로그인 시 삭제 버튼 미노출, 작성·수정·새 첨부 추가 가능 여부
  - `sky1234` 로그인 시 문서·점검·CAPA·증빙·선임·법령기록·첨부·자동백업 삭제 버튼 노출 및 실제 삭제
  - 선임자 등록의 경영책임자·안전보건관리담당자·산업보건의·명예산업안전감독관·근로자대표 삭제
- 실제 검증 결과:
  - `node --check`, `git diff --check`, `_tools/github_deploy.ps1 -CheckOnly` 통과. 임시 로컬 검증에서 마스터 비밀번호 로그인 후 경영책임자·산업보건의 행에 삭제 버튼이 표시되는 것을 확인했다.

### 2026-08-13 모든 작성창 첨부자료 기능 확장

- 수정한 파일:
  - `js/views-core.js`: 문서·점검·CAPA·증빙·선임 창에서 공통으로 쓰는 파일/링크 첨부, 열람, 삭제, 저장 준비 관리 함수 추가
  - `js/views-ext.js`: 문서체계, 이행점검, 개선조치(CAPA), 증빙 자료함, 조직·법정선임의 모든 작성창에 첨부자료 영역 연결
  - `js/core.js`: 새 문서 기본 데이터에 `attachments: []` 추가
  - `supabase/schema.sql`: `shms_documents`, `shms_inspections`, `shms_capa`, `shms_evidence`, `shms_org`의 `attachments jsonb` 생성·기존 DB 마이그레이션 추가
  - `index.html`, `js/app.js`, `js/core.js`, `js/views-core.js`, `js/views-ext.js`: 캐시버스트를 `20260813_allattachments1`로 통일
  - `작업인수인계.md`: 이번 작업 결과 기록
- 수정한 기능:
  - 법령·ISO 상세창에 있던 첨부 방식과 동일하게, 모든 일반 작성창에서 파일 또는 외부 링크를 추가·열람·삭제할 수 있다.
  - 조직·법정선임 창에서는 선임신고서·자격증·교육수료증·인사발령서를 직접 첨부할 수 있다.
  - 사진은 기존처럼 WebP 50KB 미만으로 최적화되고, 문서·ZIP은 10MB 이하만 허용한다. 파일 본문은 R2 연결 시 Worker를 통해 R2에, 미연결 시 해당 브라우저 IndexedDB에 저장한다.
  - 각 업무 행에는 첨부 메타데이터 배열만 저장한다.
- 건드리지 않은 기능:
  - 기존 법령·ISO 항목 상세창의 첨부 UX와 저장 로직, 사진 압축 기준, 문서·ZIP 제한
  - 실제 Supabase 프로젝트, R2 Worker 배포 설정 및 기존 업무 데이터
- 다음 작업자가 이어서 봐야 할 내용:
  1. Supabase 연결 전에는 `supabase/schema.sql`의 새 `attachments jsonb` 마이그레이션을 실제 SQL Editor에서 실행해야 원격 저장이 가능하다.
  2. 행 자체를 삭제할 때 첨부 원본을 함께 정리하는 정책은 이번 범위에 포함하지 않았다. R2 정식 운영 전에 보존 기간과 삭제 정책을 별도로 정한다.
- 테스트해야 할 항목:
  - 문서·점검·CAPA·증빙·선임 각각에서 파일 및 링크 추가 → 저장 → 재조회 → 보기 → 삭제
  - 선임자 등록에서 선임신고서, 자격증, 교육수료증 파일 첨부 후 재열람
  - Supabase 마이그레이션 후 원격 메타데이터 저장과 R2 원본 열람
- 실제 검증 결과:
  - `node --check`, `git diff --check`, `_tools/github_deploy.ps1 -CheckOnly` 통과. 관련 파일의 캐시버스트는 `20260813_allattachments1`로 일치한다.
  - 로컬 검증 페이지에서 선임자 등록 → 외부 링크 첨부 → 저장 → 재열기까지 수행했고, 첨부명과 `보기` 버튼이 재표시되는 것을 확인했다. 검증 페이지 파일은 삭제했다.
  - 커밋 `cc09d5d`를 `main`에 푸시했으며, GitHub Pages가 `20260813_allattachments1` 정적 파일을 응답하는 것을 확인했다.

### 2026-08-13 상세창 폭 확대 및 UI 문구 정리

- 수정한 파일:
  - `css/style.css`: 법령·의무 항목 상세창 폭을 데스크톱 `88vw`, 작은 화면 `90vw`로 조정
  - `js/views-core.js`: 상세창의 심사 관련 UI 문구를 제거하고 `심사 준비자료`를 `실행 내역`으로 변경
  - `js/app.js`: 메인 내비게이션의 `심사 대응 매트릭스`를 `ISO 45001 대응 매트릭스`로 변경
  - `index.html`, `js/app.js`, `js/core.js`, `js/views-core.js`, `js/views-ext.js`: 캐시버스트를 `20260813_drawer88_1`로 통일
  - `작업인수인계.md`: 이번 작업 결과 기록
- 수정한 기능:
  - 상세창은 데스크톱 화면 폭의 88%를 사용하므로 요청한 85~90% 범위에서 넓게 표시된다.
  - 메인 화면과 상세창에서 보이는 심사 관련 안내 문구를 이행·실행 중심 문구로 정리했다.
- 건드리지 않은 기능:
  - 법령·ISO 원문 데이터, 내부심사 등 실제 업무·법령 의미를 가진 데이터 문구, 저장·첨부·압축 로직
  - 상세창의 입력 ID, 저장 흐름, 다른 화면의 기능
- 다음 작업자가 이어서 봐야 할 내용:
  1. 실제 업무 데이터의 법령 원문에 포함된 `심사`는 UI 정리 범위에서 제외했다. 용어 자체를 전사적으로 변경해야 하면 별도 범위와 법적 표현 검토가 필요하다.
- 테스트해야 할 항목:
  - 1,500px 이상 PC 화면에서 상세창 너비가 85~90%인지, 닫기·저장·내부 스크롤이 정상인지 확인
  - 메인 내비게이션과 상세창에 남은 UI용 `심사` 문구가 없는지 확인
- 실제 검증 결과:
  - `node --check`, `git diff --check`, `_tools/github_deploy.ps1 -CheckOnly` 통과. 캐시버스트 값은 관련 파일 전체에서 `20260813_drawer88_1`로 일치한다.
  - GitHub Pages 실제 배포 화면(가로 1,581px)에서 상세창 너비 1,391.27px(정확히 88%)를 측정했고, 상세창의 `심사` UI 문구 제거와 `실행 내역` 표시를 확인했다. 브라우저 콘솔 오류는 없었다.

### 2026-08-13 첨부 상세창 고급화 및 설정·백업 운영 흐름 추가

- 수정한 파일:
  - `js/views-core.js`: 법령·의무 항목 상세창 전용 구조, 요약 지표, 기준·심사 준비·첨부·이행 기록 카드, 첨부 건수 실시간 갱신, 저장바 안내 추가
  - `css/style.css`: 상세창 전용 PC 레이아웃과 첨부 업로드 카드, 설정·백업 운영 흐름 지도 스타일 추가
  - `js/views-ext.js`: 설정·백업에 공식 접속 주소, 현재 저장 위치, 권장 운영 구조, 백업·장애 대응 흐름 추가
  - `index.html`, `js/app.js`, `js/core.js`, `js/views-core.js`, `js/views-ext.js`: 캐시버스트를 `20260813_premiumdrawer1`로 통일
  - `작업인수인계.md`: 이번 작업 범위와 검증 결과 기록
- 수정한 기능:
  - 기존 단일 열 상세창을 넓은 PC 전용 패널로 바꾸고, 상단 요약 → 기준과 요구사항 → 심사 준비자료 → 첨부자료·외부 링크 → 이행 기록 순서로 정보 계층을 재구성했다.
  - 파일 첨부와 외부 링크 등록을 나란한 작업 카드로 분리하고, 빈 상태·저장 위치·사진 50KB 기준·첨부 건수를 명확히 표시한다.
  - 범용 드로어에는 영향을 주지 않도록 상세창에만 `drawer-item` 전용 클래스를 적용하고, 범용 창을 열 때 전용 클래스를 제거한다.
  - 설정·백업에 `GitHub Pages 접속 → 공용 비밀번호/향후 Supabase Auth → Supabase 업무데이터 → Cloudflare Worker·R2 첨부 본문 → JSON·브라우저 백업` 흐름을 추가했다.
  - 현재 실제 동작(로컬 또는 Supabase, IndexedDB 또는 R2)을 런타임 상태에 따라 표시하고, 운영 전환 후 권장 구조와 혼동되지 않도록 분리했다.
- 건드리지 않은 기능:
  - 법령·ISO 마스터 데이터와 조항 `id`, 이행기록·첨부 메타데이터 구조, 사진 50KB 압축 로직, 문서 10MB 제한, Supabase/R2 저장 함수
  - 대시보드·문서·점검·CAPA 등 다른 화면의 기능과 범용 작성창 동작, 모바일 중심 레이아웃
  - 실제 Supabase·Cloudflare 계정 연결 및 배포 설정
- 다음 작업자가 이어서 봐야 할 내용:
  1. Supabase Auth·RLS와 Cloudflare Worker·R2 바인딩이 실제 운영 환경에 구성되면 설정·백업의 상태가 `원격 저장 운영 중`으로 함께 표시되는지 확인한다.
  2. 운영 부서에서 JSON 백업 파일을 보관할 사내 D드라이브의 정확한 표준 폴더를 확정하면 설정 화면 문구에 절대 경로를 추가한다.
- 테스트해야 할 항목:
  - 실제 배포 화면에서 상세창 열기, 내부 스크롤, 파일 선택, 외부 링크 등록·보기·삭제, 변경사항 저장 후 재조회
  - 설정·백업의 공식 주소 링크, Supabase/R2 연결 상태별 현재 저장 위치 문구, JSON 내려받기·복원
  - 범용 작성창을 연 뒤 상세창 전용 폭·스타일이 남지 않는지 확인
- 실제 검증 결과:
  - PC 브라우저에서 상세창 상단·첨부 구역과 설정·백업 운영 흐름을 화면 캡처로 확인했다. 상세창 입력 ID와 저장 이벤트는 유지되며, 외부 링크 추가 후 첨부 건수가 `0건 → 1건`으로 갱신되고 행이 생성되는 것을 확인했다.
  - `node --check`, `git diff --check`, `_tools/github_deploy.ps1 -CheckOnly` 통과. 화면 검증용 임시 HTML은 검증 후 삭제했다.
  - 커밋 `a401b82`를 `main`에 푸시했고, GitHub Pages 실제 배포 화면에서 상세창·설정 운영 흐름 표시와 브라우저 콘솔 오류 없음까지 확인했다.

### 2026-08-13 사진 저장 기준 50KB 미만으로 긴급 정정

- 수정한 파일:
  - `js/core.js`: 사진 저장 한도를 5MB에서 50KB로 정정하고, 위험성평가 앱의 식별성 보호선(최소 긴 변 560px·최저 품질 0.42)을 적용한 WebP 반복 압축 추가
  - `js/views-core.js`: 사진 저장 안내를 1장당 50KB 미만으로 정정
  - `cloudflare/attachment-worker/src/index.js`: R2 업로드 전 사진 50KB 서버 검증 적용
  - `cloudflare/attachment-worker/README.md`, `CLOUDFLARE_SUPABASE_구성안.md`: 운영 기준 정정
  - `index.html`, `js/app.js`, `js/core.js`, `js/views-core.js`, `js/views-ext.js`: 캐시버스트를 `20260813_photo50k1`로 통일
  - `작업인수인계.md`: 잘못 적용했던 5MB 기준을 폐기하고 정정 결과 기록
- 수정한 기능:
  - 원본 사진이 49KB를 넘으면 WebP 품질과 해상도를 반복 조정하며, 최소 긴 변 560px·최저 품질 0.42 안에서 결과가 49KB 이하가 된 경우에만 첨부 목록에 등록한다.
  - 앱 저장 검사와 Cloudflare Worker 업로드 검사는 모두 사진 최대 50KB로 이중 제한한다.
  - 기존 5MB 사진 기준은 더 이상 사용하지 않는다. 문서·ZIP의 10MB 제한은 사진과 별도이므로 유지한다.
  - 위험성평가 운영 앱을 확인한 결과, 사진은 Firestore에 본문으로 저장하지 않고 브라우저 압축 → Firebase ID 토큰 인증 → Cloudflare Worker → R2 순서로 저장한다. SHMS도 같은 원칙으로 Supabase에는 메타데이터만 두고 사진 본문은 R2에 저장한다.
- 건드리지 않은 기능:
  - 모바일 레이아웃, 문서·ZIP 10MB 기준, 첨부 링크, 중복 파일 해시, Supabase 메타데이터, 기존 법령·ISO 데이터와 조항 `id`
  - 실제 Firebase·Supabase·Cloudflare 계정과 위험성평가 앱 소스
- 다음 작업자가 이어서 봐야 할 내용:
  1. 운영 화면에서 실제 고해상도 현장 사진을 선택해 압축 결과가 50KB 미만인지 확인한다.
  2. R2 Worker 실제 배포 후 50KB 초과 사진의 직접 업로드가 HTTP 413으로 차단되는지 확인한다.
- 테스트해야 할 항목:
  - 고해상도 JPG·PNG·WebP 각각의 압축 결과 크기, 육안 식별 가능 여부, R2 저장·재열람
  - 50KB 초과 파일 서버 차단, 10MB 이하 문서 등록, 기존 첨부 중복 차단
- 실제 검증 결과:
  - `git diff --check`, 앱 JavaScript·Worker `node --check`, `_tools/github_deploy.ps1 -CheckOnly`: 통과. 캐시버스트 `20260813_photo50k1` 전체 일치.
  - Worker `wrangler deploy --dry-run`: 통과 (Upload 43.71 KiB, gzip 11.81 KiB, 실제 Worker 배포 없음).
  - 위험성평가 앱 폴더의 1,303,811바이트 증빙 JPG: 19,732바이트 WebP로 변환됨. 원본이 매우 긴 화면 캡처여서 148×1,280으로 축소되며 전체 문서의 작은 글자는 읽기 어려워질 수 있으므로, 긴 문서 화면은 사진이 아니라 PDF 첨부를 권장한다.
  - 위험성평가 가이드의 문자·QR 포함 260,466바이트 PNG: 1,024×689, 19,732바이트 WebP로 변환되고 QR·핵심 문구 식별을 확인했다.
  - 현장 사진형 2,410,788바이트 PNG: 900×600, 47,858바이트 WebP로 변환되고 인물·위험요인·설비 식별을 확인했다.

### 2026-08-13 첨부파일 용량 최적화 및 Firebase 공존 점검

- 수정한 파일:
  - `js/core.js`: 사진 압축, 파일 형식·용량 제한, SHA-256 해시 처리 및 로컬/R2 중복 저장 방지 추가
  - `js/views-core.js`: 사진 압축 진행 표시, 압축 전후 용량 표시, 같은 파일 중복 등록 차단 문구 추가
  - `cloudflare/attachment-worker/src/index.js`: Worker 측 동일 형식·용량 검증, 해시 기반 R2 객체 키 및 응답 보안 헤더 보강
  - `cloudflare/attachment-worker/README.md`, `cloudflare/attachment-worker/wrangler.toml.example`, `CLOUDFLARE_SUPABASE_구성안.md`: 운영 제한과 비용 절감 정책을 5MB 사진/10MB 문서 기준으로 갱신
  - `index.html`, `js/app.js`, `js/core.js`, `js/views-core.js`, `js/views-ext.js`: 캐시버스트 값을 모두 `20260813_compress1`로 통일
  - `작업인수인계.md`: 이번 작업 결과 기록
- 수정한 기능:
  - JPG·JPEG·PNG·WebP 사진은 브라우저에서 긴 변 최대 1,920px, WebP 품질 78%로 변환하고, 원본보다 2% 이상 작아질 때만 압축본을 저장한다.
  - 사진은 압축 후 5MB 이하, PDF·Office·한글·TXT·CSV·ZIP은 10MB 이하만 허용한다. 사진 원본 선택은 30MB까지 허용하여 압축 기회를 남긴다.
  - 허용 확장자 목록을 적용하고, SHA-256이 같은 파일은 같은 이행 항목에서 중복 등록하지 않는다. R2는 같은 항목 안에서 해시 기반 객체 키를 사용해 동일 본문을 중복 보관하지 않는다.
  - 저장 화면에는 압축 전후 용량을 표시한다. 실제 파일 본문은 기존 방침대로 R2, Supabase에는 첨부 메타데이터만 저장한다.
  - 위험성평가 앱의 Firebase 코드·데이터·설정은 수정하지 않았다. 현재 SHMS 소스에는 Firebase 호출이 없으므로 두 앱이 별도 Firebase/Supabase 프로젝트라면 사용량 할당량이 직접 합산되지 않는다.
- 건드리지 않은 기능:
  - 모바일 레이아웃, 법령·ISO 마스터 데이터와 조항 `id`, 기존 이행기록·문서·점검·CAPA·증빙 데이터 구조
  - Supabase Auth/RLS, 실제 Supabase 프로젝트, Cloudflare 계정/R2 버킷/Worker의 배포 설정
  - 위험성평가 앱의 Firebase 및 Supabase 연동 방식
- 테스트한 항목 / 결과:
  - `git diff --check`, 수정한 앱 JavaScript 4개 `node --check`: 통과
  - Worker `wrangler deploy --dry-run`: 통과 (Upload 43.52 KiB, gzip 11.76 KiB, 실제 배포 없음)
  - 로컬 브라우저에서 첨부파일 등록 → 저장 → 재조회 → 중복 등록 차단 → 삭제를 확인했고 콘솔 오류가 없었다.
  - 자동화 브라우저가 Canvas·Blob 생성을 제한하여, 대용량 합성 사진의 실제 압축률 수치는 이 환경에서 재현하지 못했다. 실제 고해상도 사진으로 운영 전 1회 확인이 필요하다.
- 다음 작업자가 이어서 봐야 할 내용:
  1. Firebase와 Supabase 양쪽에 같은 위험성평가 원본을 저장한다면 레코드별 주 저장소를 하나로 정하고, 다른 쪽은 단방향 백업만 수행한다. 양방향 동기화는 중복 비용과 데이터 충돌 위험이 있다.
  2. Firebase 사용량은 Firebase/Google Cloud 프로젝트 단위로 집계되므로 위험성평가 앱이 사용하는 Firebase 프로젝트의 예산 알림을 별도로 설정한다. Supabase와 Cloudflare R2도 각각 사용량·비용 한도를 설정한다.
  3. Supabase Auth/RLS와 R2 Worker를 실제 배포한 뒤, 5MB 이하 사진·10MB PDF/Office·30MB 이하 고해상도 사진으로 다른 PC 열람과 권한 없는 접근 차단을 검증한다.

---

### 2026-07-30 초기 구축 (V1.0.0)

**사용자 요청**
- GitHub + Supabase 조합으로 안전보건관리체계 이행 관리 앱 제작
- 목적: 중대재해처벌법·산업안전보건법 실행 및 이행 관리, ISO 45001 심사 통과
- 기준 앱: `1. 안전보건팀 관리체계 설계_안전보건팀 업무 현황_계획, 실적 등`의 대시보드 틀 계승
- 산업안전보건법 규정 + 세부 절차서·지침서 포함, **각 항마다 수기 작성 가능하도록 구성**
- 작업수칙 준수, 전문적이고 완성도 높은 구성

**수정(신규 생성)한 파일**

| 파일 | 내용 |
|---|---|
| `index.html` | 진입점. ES Module 로더, 로딩 화면 |
| `css/style.css` | 디자인 시스템 전체 (기준 앱 1의 CSS 토큰 계승 + 딥네이비 사이드바 구조) |
| `js/data/frameworks.js` | ★ 도메인 마스터 — 중처법 13개 / 산안법 19개 / ISO 45001 30개 조항 + 문서체계 46건 |
| `js/core.js` | 유틸, Supabase 설정·클라이언트, Store(로컬+원격), 인증, 통계 |
| `js/views-core.js` | 대시보드, 법령/ISO 이행관리 목록, 수기 작성 드로어 |
| `js/views-ext.js` | 문서체계, 이행점검, CAPA, 증빙자료함, 조직·선임, 심사대응 매트릭스, 설정 |
| `js/app.js` | 셸, 좌측 네비게이션, 해시 라우팅, 로그인 |
| `config.js` | Supabase 설정 주입 템플릿 (기본값은 플레이스홀더) |
| `supabase/schema.sql` | 7개 테이블 + RLS 정책 + 신규가입 트리거 + 초기 관리자 등록 SQL |
| `.github/workflows/deploy.yml` | GitHub Pages 자동 배포 (JS 문법검사 포함) |
| `.claude/launch.json` | 로컬 미리보기 서버 설정 (python http.server 8899) |
| `README.md` / `CLAUDE.md` / `.gitignore` | 사용자 문서 / AI 작업지침 / 제외 규칙 |
| `assets/` | 기준 앱 1에서 로고 2개 복사 |

**구현한 기능**
- 62개 법령·ISO 조항 각각에 대해 **이행상태·담당자·최근점검일·기한·이행현황·증빙자료·미흡사항**을 수기 작성
- 반기(`2026-H2`) 단위로 기록이 누적되어 "반기 1회 이상 점검" 이력 증빙 구조 확보
- 문서체계 46건: 목적·적용범위·본문 수기 작성 + 표준 목차 자동 삽입 + 제·개정 이력 표
- ISO 45001 심사 대응 매트릭스 (조항↔문서↔이행현황↔법령) + CSV 내려받기 + 인쇄
- 이행점검 / CAPA / 증빙자료함 / 법정 선임현황 CRUD
- Supabase 미설정 시 localStorage 단독 동작, 설정 시 원격+로컬 이중 저장
- 전체 백업 JSON 내려받기·복원

**테스트한 항목 / 결과**
- `node --check` 전체 JS 파일 6개 → 전부 통과
- 로컬 서버(`localhost:8899`)에서 11개 화면 전부 렌더링 확인 → 콘솔 오류 0건
- 로그인 → 대시보드 → 조항 카드 클릭 → 드로어 작성 → 저장 → localStorage 반영 → 카드/이행률 갱신 확인
- 문서 드로어 → 표준 목차 불러오기 → 상태 변경 → 저장 → 반영 확인
- HTML 이스케이프 확인: `<script>` 문자열 입력 시 텍스트로 표시(실행 안 됨)
- 테스트 데이터는 검증 후 localStorage에서 전부 삭제함

**확인하지 못한 것 (다음 작업자 필수 확인)**
1. **Supabase 실제 연결 검증 안 됨** — 프로젝트가 아직 생성되지 않아 `schema.sql` 실행 및
   원격 upsert/RLS 동작을 실물로 확인하지 못했습니다. 프로젝트 생성 후 반드시 다음을 확인하십시오.
   - `schema.sql` 실행 성공 여부
   - 로그인 후 조항 저장 시 `shms_records` 테이블에 행이 생기는지
   - 읽기전용 권한(`auditor`) 계정으로 저장 시도 시 RLS가 거부하는지
   - 점검 도구: `node _tools/supabase_check.mjs <URL> <anon key>`
2. **Notion 세부내역 미반영** — 사용자가 제시한 Notion 페이지(`89d4c6f2...`)는
   현재 연동된 Notion 계정(sky5224122@gmail.com 워크스페이스)에서 접근 불가(404)였습니다.
   사용자가 safeteam119 계정으로 커넥터를 재연결하기로 하였으므로, 재연결 후
   해당 페이지 내용을 `frameworks.js`에 반영해야 합니다.
3. **화면 스크린샷 미확보** — 이 시점에는 브라우저 패널이 표시되지 않아 시각 확인을 못 했습니다.
   (아래 배포 작업에서 실제 화면 확인 완료)

**건드리지 않은 것**
- 다른 앱 폴더(1~6번) 파일은 전혀 수정하지 않았습니다. `1번` 폴더에서 로고 2개만 복사했습니다.

---

### 2026-07-30 GitHub 배포 완료

**수정한 파일**
- `_tools/github_deploy.ps1` 신규 — JS 문법검사 + anon key role 검증 후 푸시
- `_tools/supabase_check.mjs` 신규 — Supabase 연결·테이블·RLS 점검 (읽기 전용, 데이터 변경 없음)
- `_tools/00_로컬서버_실행.cmd` 신규
- `.github/workflows/deploy.yml` — secrets를 `if` 조건에 직접 쓰던 방식을 env 방식으로 변경
- `README.md` — 실행 도구 절 추가
- 로컬 git 계정을 `sky5224122-jpg / sky5224122@gmail.com` 으로 변경 (GitHub 계정 일치)

**작업 중 발견해 고친 문제 3건**
1. `github_deploy.ps1` 을 UTF-8 BOM 없이 저장해 Windows PowerShell 5.1 이 한글을 깨뜨려
   파싱 오류 발생 → BOM 포함으로 재저장.
   **이 파일을 편집하면 반드시 BOM을 다시 넣어야 합니다.**
   ```powershell
   $f="_tools\github_deploy.ps1"
   $c=[IO.File]::ReadAllText($f,[Text.UTF8Encoding]::new($false))
   [IO.File]::WriteAllText($f,$c,[Text.UTF8Encoding]::new($true))
   ```
2. `config.js` 주석 안의 "service_role" 문자열을 키 노출로 잘못 판정 → 실제 anonKey 값만
   JWT 디코드해 role 을 확인하도록 수정
3. `git remote get-url origin` 이 원격이 없을 때 stderr 를 내보내 PowerShell 이 종료 오류로
   처리 → `git remote` + `git config --get` 조합으로 변경

**배포 결과 (실물 화면 확인 완료)**
- 저장소: https://github.com/sky5224122-jpg/daiso-shms (Public)
- 운영 URL: **https://sky5224122-jpg.github.io/daiso-shms/**
- GitHub Pages Source = GitHub Actions 설정 완료
- 첫 워크플로는 Pages 활성화 전에 실행되어 실패(`Get Pages site failed`) → 재실행 후 Success
- 운영 URL 접속 → 로그인 → 대시보드 렌더링까지 실제 화면으로 확인
  (62개 조항 / 46개 문서 / ISO 레이더 차트 정상 표시)

**확인하지 못한 것**
- Supabase 미연결 상태. 운영 URL도 현재 "🟡 로컬 저장" 모드로 동작함
- Notion 세부내역 미반영 (커넥터 계정 불일치로 404)

**다음 작업**
1. Supabase 프로젝트 생성 → `schema.sql` 실행 → 앱 [설정] 화면에서 URL/anon key 입력
   → `node _tools/supabase_check.mjs` 로 검증
2. Notion 세부내역 반영
3. 실제 조직 데이터 입력: 법정 선임 현황, 문서 담당자, 연간 목표
4. 필요 시 매장 단위 관리 확장 (현재는 전사 단위 관리 구조)

---

### 2026-07-31 로그인 방식 변경 (아이디 제거, 공용 비밀번호)

**사용자 요청**
- "로그인없이 사이트에 접속하면 바로 들어갈 수 있도록. 비밀번호만 입력하게 하자. s123456으로 설정"

**수정한 파일**
- `js/core.js` — `signIn()` 을 공용 비밀번호(SHA-256) 검증으로 교체, `LOCAL_ACCOUNTS` 삭제
- `js/app.js` — 로그인 화면을 비밀번호 입력 1개로 축소, Supabase 세션 자동복원 블록 제거
- `js/views-ext.js` — 설정 화면 계정정보·권한 안내 문구 갱신
- `index.html` + 모든 js — 캐시버스트 버전 `20260731_pw2`
- `_tools/github_deploy.ps1` — 캐시버스트 검사 2종 추가
- `CLAUDE.md` — 캐시버스트 규칙, 인증·권한 절 전면 개정

**진입 사양**
- 비밀번호 `s123456` (해시 `f2e5aa…4134` 가 `core.js` 의 `GATE_HASH`)
- 진입 시 `{ name:'안전보건팀', role:'safety' }` 부여 → 전 화면 작성·수정 가능

**작업 중 발견해 고친 문제 (캐시버스트 사고 2건)**
1. **하위 모듈이 갱신되지 않음** — `index.html` 의 `js/app.js?v=` 만 올려도
   `app.js` 안의 `import './core.js'` 는 쿼리 없이 해석되어 옛 캐시가 그대로 쓰였다.
   → 실제 증상: 새 로그인 화면(비밀번호 1개)은 나오는데, 누르면 옛 `core.js` 가
     "이메일(아이디)을 입력하세요" 오류를 냄.
   → 조치: `js/**/*.js` 의 **모든 상대 import 에 동일한 `?v=` 부여**.
     일부만 붙이면 같은 모듈이 두 번 로드되어 `state` 가 분리되므로 반드시 전부 동일해야 함.
2. **같은 버전으로 두 번 배포** — `20260731_pw` 로 두 번 배포해 브라우저가 첫 배포본을
   계속 사용. → 조치: `20260731_pw2` 로 갱신.
→ 두 문제 모두 `_tools/github_deploy.ps1` 이 배포 전에 자동 검사하도록 만들었습니다.
   (검사 로직 자체에도 버그가 있었음: PowerShell 이 단일 문자열을 배열로 취급하지 않아
    `$htmlVer[0]` 이 첫 글자만 반환 → `@(...)` 로 감싸 해결)

**테스트 / 결과**
- 로컬: 오답 비밀번호 차단 / 정답 진입 / 조항 저장 → 대시보드 이행률 8% 반영 확인
  (모듈 중복 로드가 없다는 증거)
- 배포본: 실제 크롬에서 `s123456` 입력 → 대시보드 진입 확인
- 배포 스크립트: 버전 불일치·동일버전 재배포 두 경우 모두 배포 차단 동작 확인

**⚠ 미해결 과제 (다음 작업자 필독)**
- 공용 비밀번호 방식은 Supabase Auth 로그인을 하지 않습니다.
  현재 `schema.sql` 의 RLS 는 `authenticated` 사용자만 쓰기를 허용하므로,
  **Supabase 를 연결하면 저장이 거부됩니다.**
  Supabase 연결 시 인증 방식을 함께 재설계해야 합니다.
- 비밀번호 해시가 Public 저장소 소스에 포함되어 있습니다. `s123456` 은 단순하여
  실질적 보호력이 없습니다. 외부 노출이 문제가 되면 저장소를 Private 로 전환하거나
  Supabase Auth 로 전환해야 합니다.

**아직 반영하지 않은 것**
- 사용자가 제공한 Notion 캡처(산안법 준수 40행 / 중처법 준수 표) 내용 미반영.
  담당자 실명·준비현황이 포함되어 Public 저장소 게시 여부 확인 후 진행 예정.

---

### 2026-08-03 산안법 항목 확장 + Notion 데이터 3개 필드 반영 + 자동 백업 기능

**사용자 요청**
1. 산안법 항목을 19개→35개로 확장하고 전 항목에 벌칙 정보 추가 (이전 세션에서 완료)
2. 자동 백업 기능 추가 (localStorage 스냅샷)
3. Notion DB의 누락 컬럼 3개 반영: 작성 및 보관자료목록, 당사 준비현황, 증빙자료

**수정한 파일**

| 파일 | 내용 |
|---|---|
| `js/data/frameworks.js` | MSSA 13개 + OSHA 35개 = 48개 항목에 `requiredDocs`, `companyStatus`, `evidenceFiles` 3개 필드 추가. 실명 제외, 법령·운영정보만 수록 |
| `js/views-core.js` | `openItemDrawer()` 에 📋 작성 및 보관자료 목록 / 🏢 당사 준비현황 / 📁 증빙자료 ref-box 3개 추가 |
| `js/core.js` | 자동 백업 시스템 추가 (`autoBackup`, `getBackups`, `restoreBackup`, `deleteBackup`). 저장 시 1분 간격 debounce, 최대 5개 보관 |
| `js/views-ext.js` | 설정 페이지에 🔄 자동 백업(최근 5개) 관리 UI (복원/삭제) 추가 |
| `index.html`, `js/app.js` | 캐시버스트 `20260731_bk1` → `20260803_n1` |

**Notion 데이터 반영 방식**
- 사용자가 공유한 Notion 캡처 이미지 13장에서 데이터 추출
- Node.js 스크립트(`add_notion_fields.mjs`)로 48개 항목에 일괄 삽입
- 실명은 제외하고 법령정보·운영정보만 소스에 포함 (사용자 지시)

**테스트 / 결과**
- 로컬 서버(8899)에서 산안법 목록 35개 조항 정상 표시 확인
- 법 제14조 드로어 열기 → 3개 신규 필드 모두 정상 표시 확인
- JS 문법검사(`node --check`) 5개 파일 전부 통과
- 설정 페이지 백업 UI 정상 렌더링 확인

**건드리지 않은 기능**
- ISO 45001 항목 (30개) — `requiredDocs`/`companyStatus`/`evidenceFiles` 미추가 (Notion 데이터 없음)
- Supabase 연결·RLS — 미변경
- 대시보드 통계 로직 — 미변경

**확인하지 못한 것**
- 운영 URL(GitHub Pages)에서 캐시버스트 반영 여부 (push 직후이므로 빌드 완료 대기 필요)
- 48개 항목 전부의 드로어 개별 확인은 미실시 (제14조 1건만 실제 확인)

**다음 작업자가 봐야 할 것**
1. 운영 URL에서 캐시버스트 `20260803_n1` 반영 확인
2. ISO 항목에도 Notion 데이터가 있다면 동일 3개 필드 추가 필요
3. Supabase 연결 시 인증 방식 재설계 (미해결 과제 유지)

---

### 2026-08-12 Notion 3개 필드 수기편집 전환 + 첨부파일 관리 UI 추가

**사용자 요청**
1. Notion 이미지 12장의 companyStatus·evidenceFiles 데이터를 48개 항목에 갱신 (실명 제외)
2. 드로어의 작성및보관자료/당사준비현황/증빙자료를 **수기편집 가능한 textarea**로 전환
3. 항목별 **첨부파일 등록** 기능 추가 (파일명·문서번호 텍스트 + 비고)
4. 심사 대응 및 일상 업무 운영용으로 지속 활용 목적

**수정한 파일**

| 파일 | 내용 |
|---|---|
| `js/views-core.js` | `openItemDrawer()`: 읽기전용 ref-box 3개 → 편집 가능 textarea 3개 전환 + 첨부파일 추가/삭제 UI 및 이벤트 핸들러 추가. 저장 시 `userDocs`/`userStatus`/`userEvidence`/`attachments` 포함 |
| `js/core.js` | `getRecord()` 기본값에 `userDocs`, `userStatus`, `userEvidence`, `attachments` 4개 필드 추가 |
| `js/data/frameworks.js` | Notion 이미지 12장 기반으로 48개 항목의 `companyStatus`·`evidenceFiles` 갱신 + MSSA-4-8 `linkedApp` 구조 버그 수정 (닫는 중괄호 누락) |
| `css/style.css` | 첨부파일 UI 스타일 추가 (`.attach-list`, `.attach-row`, `.attach-add` 등) |
| `index.html`, `js/app.js`, `js/views-ext.js` | 캐시버스트 `20260811_pw` → `20260812_ed` |

**설계 판단**
- frameworks.js 기본값을 textarea에 미리 표시하되, 사용자가 편집하면 레코드(userDocs 등)에 별도 저장 → 원본 데이터 보존
- 첨부파일은 실제 파일 업로드가 아닌 **파일명·문서번호 텍스트 기록** 방식 (Supabase 미연결 상태에서 가용한 방식)
- 첨부파일 배열은 `[{name, note, date}]` 구조로 레코드에 저장

**테스트한 항목 / 결과**
- 로컬 서버(port 8899)에서 OSHA-014 항목 드로어 열기 → textarea 3개 + 첨부파일 UI 정상 렌더링 확인
- frameworks.js 기본값(작성및보관자료, 당사준비현황, 증빙자료) 정상 표시 확인
- 첨부파일 추가 → 목록 렌더링 확인
- 저장 → localStorage에 userDocs/userStatus/userEvidence/attachments 정상 저장 확인
- 콘솔 에러 없음

**확인하지 못한 것**
- 48개 항목 전체 드로어 개별 확인은 미실시 (OSHA-014 1건만 실제 확인)
- 운영 URL(GitHub Pages) 배포 반영 여부 (push 직후이므로 빌드 완료 대기 필요)
- 첨부파일 삭제 기능 클릭 테스트 미실시 (코드 구조상 정상 동작 예상)

**다음 작업자가 봐야 할 것**
1. 운영 URL에서 캐시버스트 `20260812_ed` 반영 확인
2. 실제 파일 업로드가 필요하면 Supabase Storage 연동 설계 필요
3. Supabase 연결 시 `userDocs`/`userStatus`/`userEvidence`/`attachments` 컬럼을 `shms_records` 테이블에 추가해야 함
4. Supabase 연결 시 인증 방식 재설계 (미해결 과제 유지)

---

### 2026-08-13 전역 스피너 및 이행항목 카드 디자인 개선 인수인계 반영

- 수정한 파일:
  - `작업인수인계.md` — 커밋 `96be8fd`, `4c940c3`의 작업 결과와 후속 확인 사항 기록
  - `_context_handoff_20260813_1200.md` — 다음 작업용 상세 인수인계 문서를 저장소 체크포인트에 포함
- 수정한 기능:
  - 이번 인수인계 적용 과정에서 앱 기능 코드는 수정하지 않음
  - 기존 커밋 `96be8fd`: 자료 로딩과 주요 저장 흐름에 전역 스피너 표시 적용
  - 기존 커밋 `4c940c3`: 이행항목 카드를 헤더, 이행률, 본문, 팩트 그리드, 연계정보의 5단 정보 계층으로 개편하고 관련문서·ISO·벌칙·자료보유 칩을 강조
- 건드리지 않은 기능:
  - `js/data/frameworks.js`의 조항 `id` 및 법령·ISO 마스터 데이터
  - 저장 구조, localStorage 데이터, Supabase 스키마·인증·RLS
  - 증빙자료 공란 24개 및 기타 실제 회사 자료
  - 카드·스피너를 포함한 앱 소스와 캐시버스트 버전
- 테스트한 항목 / 결과:
  - `git status` 및 최근 커밋 확인: 인수인계 작성 전 앱 소스의 추적 변경 없음
  - 커밋 `96be8fd`, `4c940c3` 존재 및 인수인계 문서의 설명과 일치 여부 확인
  - `CLAUDE.md`의 조항 id·캐시버스트·인증·작업 기록 규칙 확인
  - 이번 작업은 문서 반영만 수행하여 앱 문법검사·화면·저장/조회 재테스트는 수행하지 않음
- 확인하지 못한 것:
  - 운영 OSHA 페이지에서 벌칙 칩이 실제 데이터와 함께 정상 표시되는지 재검증하지 않음
  - 카드 디자인에 대한 사용자 최종 피드백 미확인
  - Supabase는 여전히 미연결 상태이며 공용 비밀번호 인증과 RLS 충돌 미해결
- 다음 작업자가 봐야 할 것:
  1. 사용자 요청이 있을 때 OSHA 페이지에서 벌칙 칩을 실제 브라우저로 검증
  2. 증빙자료 공란 24개는 실제 회사 자료이므로 임의 생성하지 말고 사용자가 입력하도록 유지
  3. Supabase 연결 작업 전 인증 방식과 `shms_can_edit()` 정책을 함께 재설계
  4. 앱 소스 수정 시 5개 진입/import 파일의 캐시버스트 값을 모두 동일한 새 값으로 변경

---

### 2026-08-13 운영 사이트 보강 항목 점검

- 수정한 파일:
  - `작업인수인계.md` — 운영 사이트 점검 결과와 보강 우선순위 기록
- 수정한 기능:
  - 앱 기능 코드는 수정하지 않음
  - 운영 URL의 데스크톱·모바일 표시, 산안법 카드, 작성 드로어, 대시보드, 설정·백업 화면을 읽기 전용으로 점검
- 건드리지 않은 기능:
  - 이행기록·문서·점검·CAPA·증빙·조직 데이터
  - localStorage 및 Supabase 설정
  - 법령·ISO 마스터 데이터와 조항 `id`
  - 인증, 저장, 캐시버스트, 배포 소스
- 테스트한 항목 / 결과:
  - 운영 산안법 화면: 35개 카드 모두 벌칙 칩 표시(35/35), 관련문서 누락 없음, 콘솔 경고·오류 없음
  - 법 제14조 작성 드로어: 수기편집 필드, 첨부파일 등록란, 이행상태·담당·날짜·이행현황·증빙·미흡사항 입력란과 저장 버튼 정상 표시
  - 모바일 390×844: 사이드바가 268px로 열린 채 유지되어 본문 폭이 112px로 축소되고 가로 넘침 162px 발생 — 모바일 레이아웃 보강 필요
  - 설정 화면: 현재 브라우저 전용 localStorage 모드, 이행기록 0건·자동백업 0건으로 표시됨
  - 산안법 데이터 표시: 준비현황 미기재 6개, 증빙자료 0개 표시 14개 확인
- 확인하지 못한 것:
  - 데이터 변경 방지를 위해 실제 저장·백업 내려받기·복원은 실행하지 않음
  - 법령 문구와 벌칙 금액의 최신 법률 원문 대조는 이번 점검 범위에서 수행하지 않음
- 다음 작업자가 봐야 할 것:
  1. 최우선: Supabase Auth와 `shms_can_edit()` RLS를 함께 재설계하고 신규 레코드 필드 4개(`userDocs`, `userStatus`, `userEvidence`, `attachments`)를 스키마에 반영한 뒤 공유 저장을 연결
  2. 모바일: 820px 이하에서 사이드바를 기본 오프캔버스 또는 자동 축소하고 도구막대·필터·카드를 1열로 재배치
  3. 개인정보: `js/views-core.js` 담당자 예시의 실명 `강동현`을 부서명 중심의 비식별 예시로 교체
  4. 운영정보: 앱 버전 `V1.0.0 (2026-07-30)`을 실제 배포 버전과 맞추거나 빌드·배포일 자동 표시 검토
  5. 실제 회사 자료인 증빙 공란은 임의 생성하지 말고 사용자 입력 또는 검증된 원본 일괄 등록 방식으로 보완

---

### 2026-08-13 Supabase·Cloudflare 분리 구성 및 첨부파일·링크 기능

- 수정한 파일:
  - `js/core.js` — 첨부 저장 어댑터(Cloudflare R2 Worker / 로컬 IndexedDB), 파일 열람·삭제, 링크 검증, Supabase 레코드 신규 필드 매핑 추가
  - `js/views-core.js` — 개인 실명 예시 제거, 담당자·부서 수기 입력 유지, 파일·링크 등록/보기/삭제 UI 추가
  - `js/views-ext.js` — Supabase Auth/RLS 선행 필요 경고와 현재 첨부파일 저장 모드 표시
  - `css/style.css` — 파일·링크·기존 기록 구분 및 첨부 입력 UI 스타일
  - `supabase/schema.sql` — `user_docs`, `user_status`, `user_evidence`, `attachments jsonb` 신규/기존 DB 마이그레이션 추가
  - `config.js` — 공개 Worker URL용 `SHMS_ATTACHMENT_API` 플레이스홀더 추가
  - `index.html`, `js/app.js`, `js/core.js`, `js/views-core.js`, `js/views-ext.js` — 캐시버스트 `20260813_attach1`로 통일
  - `cloudflare/attachment-worker/*` — Supabase JWT 검증 후 R2 업로드·열람·삭제를 처리하는 Worker 배포 골격
  - `CLOUDFLARE_SUPABASE_구성안.md` — 역할 분리, 공식 한도, 적용 순서와 비용관리 원칙
  - `.gitignore` — Worker 로컬 상태와 실제 `wrangler.toml` 제외
- 수정한 기능:
  - 담당자 입력 예시의 개인 이름을 삭제하고 `담당자명 또는 담당부서를 직접 입력하세요`로 변경
  - 링크(URL·표시명·비고)와 20MB 이하 파일을 같은 이행항목에 등록 가능
  - 기존 파일명 텍스트 기록은 `기록` 유형으로 호환 유지
  - Cloudflare Worker 미설정 시 실파일은 현재 브라우저 IndexedDB에 저장하고 재접속 후 목록·메타데이터 유지
  - Worker 설정 및 Supabase Auth 세션이 있으면 같은 UI에서 R2 저장·열람·삭제 경로 사용
  - Supabase에는 구조화 데이터와 첨부 메타데이터만 저장하고 실제 파일은 R2로 분리하는 구성 확정
- 건드리지 않은 기능:
  - 모바일 레이아웃 — 사용자 지시에 따라 이번 작업 범위에서 제외
  - 법령·ISO 마스터 데이터 및 조항 `id`
  - 기존 이행기록·문서·점검·CAPA·증빙·조직 데이터
  - 운영 Supabase 프로젝트와 Cloudflare 계정 — 실제 외부 배포·키 설정 없음
  - 공용 비밀번호 인증 — 실제 Supabase Auth 전환은 계정 구성 후 후속 작업으로 유지
- 테스트한 항목 / 결과:
  - `node --check`: 앱 JS 5개와 Worker JS 통과
  - `_tools/github_deploy.ps1 -CheckOnly`: 키 노출·캐시버스트 검사 통과
  - Worker `wrangler deploy --dry-run`: 번들 42.47KiB, gzip 11.44KiB로 빌드 통과
  - npm 의존성 감사: 취약점 0건
  - 로컬 브라우저: 개인 이름 없는 담당자 placeholder, 파일/링크 입력 UI 표시 확인
  - 테스트 링크와 69B 텍스트 파일 등록 → 저장 → 드로어 재열기 후 두 항목 재조회 → 삭제 → 재저장 확인
  - 설정 화면에 `현재 브라우저 IndexedDB 전용` 표시 및 Supabase Auth 선행 경고 확인
  - 최종 콘솔 경고·오류 없음, 테스트 데이터와 임시 파일 정리 완료
  - GitHub Actions 배포 실행 `31679749129` 성공
  - 운영 URL HTTP 200, 캐시버스트 `20260813_attach1`, `saveAttachmentFile` 코드, 비식별 담당자 입력 문구 반영 확인
- 확인하지 못한 것:
  - 자동화 브라우저가 `blob:` 새 창 이동을 보안상 차단하여 로컬 파일의 실제 새 창 열람은 자동 확인하지 못함
  - 실제 Supabase Auth 로그인, RLS, R2 업로드·다른 PC 열람·R2 삭제는 계정/키 미설정으로 미검증
  - Cloudflare Access는 실제 사용자 수가 50명 이하인지 확인 후 적용 여부 결정 필요
- 다음 작업자가 봐야 할 것:
  1. `CLOUDFLARE_SUPABASE_구성안.md` 순서대로 Supabase Auth와 RLS부터 구축
  2. `schema.sql` 마이그레이션 실행 후 두 권한 계정으로 읽기·쓰기 차단 검증
  3. Cloudflare R2 버킷과 Worker를 배포하고 `config.js`의 공개 Worker URL 설정
  4. 실제 PDF·Office·사진 파일로 업로드, 새 창 보기/다운로드, 다른 PC 열람, 삭제 권한 검증
  5. Access 대상이 50명을 넘으면 사용자당 비용이 발생하므로 앱 사용자 범위를 확정한 뒤 적용

---

### 2026-08-14 Supabase 공동 운영 로그인 및 일일 백업 전환

- 수정한 파일:
  - `config.js` — 운영 Supabase Project URL과 publishable(공개) 키 연결
  - `js/core.js` — Supabase Auth 회원가입·로그인·세션 복원, 프로필 역할 조회, master 전용 삭제 판정 및 매일 오전 9시 브라우저 백업 추가
  - `js/app.js` — 이메일 계정 기반 로그인/회원가입 화면 및 일일 백업 예약 연결
  - `js/views-ext.js` — 설정 화면의 인증 안내를 Supabase 공동 운영 방식으로 표시
  - `supabase/schema.sql` — 가입자는 작성·수정 가능, `master` 역할만 삭제 가능한 RLS 기본 스키마 반영
  - `supabase/공동운영_권한전환.sql` — 이미 생성된 Supabase 프로젝트를 공동 운영 RLS 정책으로 전환하는 실행 스크립트
  - `index.html`, `js/app.js`, `js/core.js`, `js/views-core.js`, `js/views-ext.js` — 캐시버스트 `20260814_sharedauth1`로 통일
  - `작업인수인계.md` — 이번 전환 결과 기록
- 수정한 기능:
  - 각 사용자는 이메일·비밀번호로 가입/로그인하고, 같은 Supabase 데이터를 함께 조회·작성·수정
  - 삭제는 서버 RLS와 앱 화면 모두 `master` 역할 사용자만 가능
  - 자동 백업은 앱이 열린 상태에서 매일 오전 9시 1회만 브라우저에 생성하며 최근 5개만 유지 (Supabase 사용량 증가 없음)
  - 2026-08-14 Supabase SQL Editor에서 `공동운영_권한전환.sql` 실행 성공 화면을 확인
- 건드리지 않은 기능:
  - 법령·ISO 마스터 데이터와 조항 `id`
  - 기존 이행기록·문서·점검·CAPA·증빙·조직의 업무 화면과 데이터 구조
  - Cloudflare R2 Worker 실제 배포 및 버킷 생성
  - 모바일 화면 구조
- 다음 작업자가 이어서 봐야 할 내용:
  1. 배포 앱에서 첫 관리자 계정을 만든 뒤 `shms_profiles`의 해당 이메일 `role`을 `master`로 한 번 변경해야 삭제 권한이 생긴다. 사용자 이메일을 임의로 추정하지 말 것.
  2. R2 Worker가 아직 미연결이므로 첨부 파일 원본은 각 브라우저 IndexedDB에만 저장된다. 파일까지 공동 열람하려면 Cloudflare R2/Worker 배포와 `config.js` Worker URL 설정이 필요하다.
  3. 최소 두 개의 서로 다른 브라우저 계정으로 기록 작성·수정 공유와 master/일반 사용자 삭제 차단을 실제 검증할 것.
- 테스트해야 할 항목:
  - Supabase Auth 설정 API가 publishable 키로 HTTP 200 응답하는지 확인
  - 새 계정 가입, 이메일 인증(프로젝트 설정에 따라 필요), 재로그인 및 다른 브라우저에서 동일 기록 조회
  - 일반 사용자 작성·수정 가능 / 삭제 차단, master 사용자 삭제 가능
  - 오전 9시 앱 실행 상태의 자동 백업 1회 생성 및 최대 5개 유지
  - R2 연결 후 사진·PDF 업로드, 다른 PC 열람 및 삭제 권한

---

### 2026-08-14 이메일 대신 사용자 아이디 로그인 전환

- 수정한 파일:
  - `js/app.js` — 로그인·가입 입력을 이메일에서 사용자 아이디로 변경
  - `js/core.js` — 아이디를 Supabase 내부 이메일 별칭으로 변환하고 `login_id` 프로필을 복원
  - `js/views-ext.js` — 설정 화면의 인증 안내를 아이디 로그인 기준으로 변경
  - `supabase/schema.sql` — `shms_profiles.login_id`와 신규 가입 트리거 반영
  - `supabase/아이디_계정전환.sql` — 기존 프로젝트에 추가 실행할 아이디 컬럼·트리거 마이그레이션
  - `index.html`, `js/app.js`, `js/core.js`, `js/views-core.js`, `js/views-ext.js` — 캐시버스트 `20260814_userid1`로 갱신
- 수정한 기능:
  - 화면에서 `master`, `safety02`, `health01` 같은 아이디로 로그인 가능
  - Supabase 내부에서는 `아이디@accounts.daiso-shms.local` 별칭을 사용하며 사용자에게 이메일 입력을 요구하지 않음
  - 프로필에는 `login_id`가 저장되고, 삭제 권한은 기존처럼 `master` 역할만 보유
- 건드리지 않은 기능:
  - 업무 데이터 테이블과 법령·ISO 마스터 데이터
  - 첨부파일의 R2 Worker 연결, 모바일 레이아웃
  - 비밀번호 값 — 소스·SQL에 기록하지 않음
- 다음 작업자가 이어서 봐야 할 내용:
  1. Supabase SQL Editor에서 `supabase/아이디_계정전환.sql`을 1회 실행한다.
  2. Supabase Auth의 이메일 확인(Confirm email)이 켜져 있으면 내부 별칭으로 확인 메일을 받을 수 없으므로, 아이디 계정 운영 시 확인 기능을 끄거나 관리자 방식으로 계정을 생성해야 한다.
  3. 각 아이디 계정을 만든 뒤 `login_id = 'master'`인 강동현 프로필만 `role = 'master'`로 변경한다.
  4. 공개 회원가입을 계속 둘 경우 중복 아이디 선점 가능성이 있으므로, 계정 생성 완료 후 Supabase Auth의 신규 가입 허용을 끄는 것을 권장한다.
- 테스트해야 할 항목:
  - 아이디 입력란과 계정 만들기 화면 표시
  - `login_id` 프로필 생성 및 아이디 로그인
  - 일반 직원 계정의 작성·수정 가능 / 삭제 차단
  - `master` 계정의 삭제 가능
  - 중복 아이디 가입 차단 및 인증 설정에 따른 가입 결과

---

### 2026-08-14 작성·수정·삭제 변경 이력 추가

- 수정한 파일:
  - `js/core.js` — 저장·삭제 시 감사 로그 기록, Supabase 조회 및 로컬 폴백 추가
  - `js/views-ext.js` — 설정 화면에 최근 변경 이력 표 추가
  - `supabase/schema.sql` — `shms_audit_log` 테이블과 조회·기록·master 삭제 RLS 추가
  - `supabase/공동운영_권한전환.sql`, `supabase/아이디_계정전환.sql` — 기존 프로젝트용 감사 로그 마이그레이션 추가
  - `index.html`, `js/app.js`, `js/core.js`, `js/views-core.js`, `js/views-ext.js` — 캐시버스트 `20260814_audit1`로 갱신
- 수정한 기능:
  - 이행기록·문서·점검·개선조치·선임·증빙의 작성·수정·삭제 시 사용자 아이디, 이름, 시각, 작업 종류, 대상 ID 기록
  - 설정 화면에서 최근 100건의 변경 이력을 공통으로 조회
  - Supabase 미연결 시 현재 브라우저에 임시 기록, 연결 후 `shms_audit_log`에 공동 저장
- 건드리지 않은 기능:
  - 기존 업무 데이터 필드와 법령·ISO 마스터 데이터
  - 첨부파일 저장 경로와 모바일 레이아웃
  - 사용자 비밀번호 및 실제 Auth 계정 생성
- 다음 작업자가 이어서 봐야 할 내용:
  1. Supabase SQL Editor에서 `supabase/아이디_계정전환.sql`을 실행하면 아이디 컬럼과 감사 로그 테이블이 생성된다.
  2. 실제 `master`, `safety02`, `safety03`, `safety04`, `health01`, `health02` Auth 계정은 각 아이디로 앱에서 생성해야 한다. 비밀번호는 코드에 기록하지 않는다.
  3. 모든 계정 생성 후 같은 SQL을 다시 실행하여 이름을 반영하고 `master`만 master 역할로 지정한다.
- 테스트해야 할 항목:
  - 서로 다른 아이디로 저장 후 설정 화면에 사용자·시각·작업이 표시되는지
  - 일반 계정 삭제 차단과 master 계정 삭제 및 삭제 이력 기록
  - Supabase RLS 적용 후 다른 사용자가 같은 이력을 조회하는지
  - SQL 미실행 상태에서도 앱이 기존 자료를 정상 표시하는지

---

### 2026-08-14 Supabase 통합 통계·미흡사항·개선 분석 보고서

- 수정한 파일:
  - `js/views-core.js` — 종합대시보드의 통계·개선 분석 보고서 생성과 열기/닫기 버튼 추가
  - `css/style.css` — 분석 보고서의 경영 보고용 카드·통계·우선조치 목록 스타일 추가
  - `index.html`, `js/app.js`, `js/core.js`, `js/views-core.js`, `js/views-ext.js` — 캐시버스트 `20260814_analytics1`로 갱신
  - `작업인수인계.md` — 구현·검증 항목 기록
- 수정한 기능:
  - 종합대시보드 상단에 `통계 · 개선 분석` 버튼 추가
  - Supabase 공동 데이터 기준으로 법령·표준별 이행률, 상태 분포, 등록 이행기록, 미흡사항, 증빙 공란, 기한 초과 CAPA, 문서 승인 전 건수 집계
  - 미이행·보완 필요 항목을 중대성 우선으로 보여주고, 항목별 확인 내용·개선 방법·요구사항을 제시
  - CAPA 상태별 실행력과 담당자 미지정·기한 임박 등 관리 누락 신호 표시, 바로 CAPA 화면으로 이동 가능
- 건드리지 않은 기능:
  - 기존 이행기록·문서·CAPA 입력 필드와 저장 구조
  - Supabase RLS, 첨부파일 R2 설정, 계정·비밀번호 값
  - 모바일 구조
- 다음 작업자가 이어서 봐야 할 내용:
  1. Supabase에 실제 공동 데이터가 쌓일수록 분석 결과가 자동으로 구체화된다. 현재 초기 데이터가 적으면 0건 중심으로 보이는 것은 정상이다.
  2. 감사 로그와 아이디 계정 전환 SQL은 기존 계획대로 SQL Editor에서 실행해야 공동 운영 이력이 서버에 저장된다.
  3. 경영진 보고서 PDF 인쇄는 기존 대표이사 보고서 기준이다. 통계·개선 분석만 별도 인쇄하는 요구가 생기면 전용 인쇄 버튼을 추가한다.
- 테스트해야 할 항목:
  - 종합대시보드의 `통계 · 개선 분석` 버튼 열기/닫기
  - 미이행·보완·기한 임박·증빙 공란 데이터 입력 시 수치와 우선목록 갱신
  - CAPA 등록·종결 시 상태별 실행력과 종결률 갱신
  - 다른 사용자 로그인 후 Supabase 공통 데이터 기준으로 동일 분석 결과 표시

---

### 2026-08-14 운영 화면 초기 로딩 멈춤 긴급 수정

- 수정한 파일:
  - `js/views-ext.js` — 문서체계 화면 템플릿 문자열의 종료 구문 복구
  - `index.html`, `js/app.js`, `js/core.js`, `js/views-core.js`, `js/views-ext.js` — 캐시버스트 `20260814_hotfix1`로 갱신
  - `작업인수인계.md` — 장애 원인·수정·검증 결과 기록
- 수정한 기능:
  - `views-ext.js` 구문 오류로 전체 앱이 `시스템을 불러오는 중입니다…`에서 멈추던 문제 해결
- 건드리지 않은 기능:
  - Supabase 설정·RLS·계정·데이터·통계 계산 로직
  - 이행기록·문서·CAPA 입력 기능 및 첨부파일 처리
- 다음 작업자가 이어서 봐야 할 내용:
  1. 배포 후 Chrome 실제 화면이 로그인 화면으로 전환되는지 확인한다.
  2. 향후 템플릿 문자열을 수정할 때는 `node --check`만으로 충분하지 않을 수 있으므로, 실제 Chrome 콘솔 오류와 화면 전환을 함께 확인한다.
- 테스트해야 할 항목:
  - Chrome 콘솔의 `Unexpected token 'class'` 오류가 사라졌는지
  - 운영 URL에서 초기 로딩 화면 이후 아이디 로그인 화면 표시
  - 로그인 후 문서체계·대시보드·통계·개선 분석 화면 정상 진입

---

### 2026-08-14 Supabase 로그인 연결 우선순위 수정

1. 수정한 파일
   - `js/core.js`: 배포된 `config.js`의 공식 Supabase 연결을 브라우저에 남아 있던 임시 설정값보다 우선하도록 변경
   - `index.html`, `js/app.js`, `js/core.js`, `js/views-core.js`, `js/views-ext.js`: 캐시버스트를 `20260814_loginfix1`로 일치 갱신
2. 수정한 기능
   - 이전 Supabase 프로젝트 정보가 브라우저 `localStorage`에 남아 있어도 현재 배포 앱은 공식 `daiso-shms` 프로젝트로 로그인한다.
3. 건드리지 않은 기능
   - 이행기록·문서·CAPA·첨부·통계·삭제권한·RLS 및 계정 구조는 변경하지 않았다.
4. 다음 작업자가 이어서 봐야 할 내용
   - Supabase Auth 사용자 6개는 생성되어 있고, `master` 계정의 비밀번호를 SQL Editor에서 재설정했다.
   - 계정 생성 후 `아이디_계정전환.sql`의 이름·역할 UPDATE를 다시 실행해야 `master` 삭제권한과 직원 이름이 서버 프로필에 반영된다.
5. 테스트해야 할 항목
   - 배포 후 새 주소 `?v=20260814_loginfix1`에서 `master` 로그인 성공 여부
   - 일반 직원 로그인, 공동 조회·작성, master 삭제권한과 일반 계정 삭제 차단 여부

---

### 2026-08-14 GitHub Pages 공식 Supabase 연결 보조값 추가

1. 수정한 파일
   - `js/core.js`: `config.js`가 브라우저에서 실행되지 않는 경우에도 공식 `daiso-shms` Supabase 프로젝트로 연결하는 공개 publishable 키 보조값 추가
   - `index.html`, `js/app.js`, `js/core.js`, `js/views-core.js`, `js/views-ext.js`: 캐시버스트를 `20260814_loginfix2`로 일치 갱신
2. 수정한 기능
   - `master` 계정이 정상인데도 브라우저의 이전 연결값 때문에 `Invalid login credentials`가 표시되던 로그인 경로를 공식 프로젝트로 고정
3. 건드리지 않은 기능
   - 계정 비밀번호·RLS 정책·데이터 테이블·첨부·이행기록·문서·CAPA·통계 화면은 변경하지 않았다.
4. 다음 작업자가 이어서 봐야 할 내용
   - `config.js`가 브라우저에서 실행되지 않는 상황을 실제 Chrome에서 확인했다. 핵심 연결값은 `core.js` 보조값으로도 유지된다.
   - Supabase `shms_profiles`의 `master` 행은 현재 `role=safety`, 이름은 내부 이메일 값이다. 계정 생성 후 프로필 이름·역할 반영 SQL이 아직 필요하다.
5. 테스트해야 할 항목
   - 운영 URL `?v=20260814_loginfix2#/dashboard`에서 master 로그인 성공 및 대시보드 진입
   - 직원 5명 로그인과 작성·수정, master 삭제권한, 일반 계정 삭제 차단

---

### 2026-08-14 Supabase 계정·권한 실제 적용

1. 수정한 파일
   - `작업인수인계.md`: 실제 Supabase 계정·권한 적용 및 로그인 진단 결과 기록
2. 수정한 기능
   - Supabase Auth에서 `master`, `safety02`, `safety03`, `safety04`, `health01`, `health02` 계정 생성을 확인했다.
   - `master` 비밀번호를 재설정했고, 서버 인증 API로 정상 인증(HTTP 200)을 확인했다.
   - `shms_profiles`에 강동현(master), 박찬욱, 유준하, 서유림, 박윤하, 윤정인 이름과 역할을 실제 적용했다. master만 `master`, 나머지는 `safety` 역할이다.
3. 건드리지 않은 기능
   - 데이터·첨부·통계·보고서·RLS 정책 구조는 변경하지 않았다.
4. 다음 작업자가 이어서 봐야 할 내용
   - Chrome 자동화의 로그인 버튼 동작은 화면 전환으로 재현되지 않았으나, 동일 계정·비밀번호로 Supabase Auth API 인증은 HTTP 200으로 확인됐다. 실제 사용자 클릭으로 운영 URL에서 최종 확인이 필요하다.
5. 테스트해야 할 항목
   - `https://sky5224122-jpg.github.io/daiso-shms/?v=20260814_loginfix2#/dashboard`에서 master 로그인 후 대시보드 표시
   - 일반 직원 로그인, 공동 저장·조회, master 삭제 가능 및 일반 계정 삭제 차단

---

### 2026-08-14 로그인 화면 버전 표기 및 마스터 전용 시스템 정보

1. 수정한 파일
   - `js/app.js`: 로그인 화면 버전 표기에서 날짜 제거
   - `js/views-ext.js`: 설정·백업 화면의 시스템 버전을 master에게만 표시
   - `index.html`, `js/app.js`, `js/core.js`, `js/views-core.js`, `js/views-ext.js`: 캐시버스트를 `20260814_versioninfo1`로 일치 갱신
2. 수정한 기능
   - 로그인 화면에는 `V1.0.0`만 표시한다.
   - 로그인 후 `설정 · 백업` 화면의 시스템 버전 정보는 master 권한 사용자만 볼 수 있다.
3. 건드리지 않은 기능
   - 로그인 방식·계정·권한·저장·첨부·백업·데이터 화면은 변경하지 않았다.
4. 다음 작업자가 이어서 봐야 할 내용
   - 실제 master와 일반 직원 계정으로 설정 화면을 각각 확인해 마스터 전용 행이 정확히 숨겨지는지 확인한다.
5. 테스트해야 할 항목
   - 로그인 화면에 날짜가 보이지 않는지
   - master의 설정·백업 화면에는 `시스템 버전 (마스터 전용)`이 보이고, 일반 계정에는 보이지 않는지

---

### 2026-08-14 Cloudflare R2 공동 첨부 운영 실제 연결 완료

1. 수정한 파일
   - `config.js`: 배포된 Cloudflare Worker 공개 주소 연결
   - `js/core.js`: `config.js` 실행 여부와 무관하게 공식 R2 Worker 주소를 사용하는 보조값 추가
   - `index.html`, `js/app.js`, `js/core.js`, `js/views-core.js`, `js/views-ext.js`: 캐시버스트를 `20260814_r2ready1`로 일치 갱신
   - `cloudflare/attachment-worker/wrangler.toml`: 실제 Cloudflare 배포 설정 파일 생성(저장소 제외 파일)
2. 수정한 기능
   - Cloudflare R2 버킷 `daiso-shms-files` 생성
   - Worker `daiso-shms-attachments` 배포: `https://daiso-shms-attachments.sky5224122.workers.dev`
   - Supabase JWT 인증 후 R2 업로드·열람·삭제 경로를 실제 연결
   - 시험 PNG로 업로드(201) → 열람(200) → 삭제(200)를 실제 확인
   - Worker에서 master 역할을 서버 프로필로 확인하도록 보완: 직원이 올린 시험 파일을 master가 삭제(200), 직원의 타인 파일 삭제는 차단(403) 확인
3. 건드리지 않은 기능
   - 이행기록·문서·CAPA·점검·선임 데이터 구조, 기존 권한 정책, 사진 50KB/문서 10MB 제한은 변경하지 않았다.
4. 다음 작업자가 이어서 봐야 할 내용
   - 실제 운영 첨부는 이제 R2에 저장되며, Supabase에는 메타데이터만 저장된다.
   - 기존에 각 PC 브라우저 IndexedDB에만 있던 첨부 원본은 자동으로 R2로 옮겨지지 않는다. 필요하면 해당 파일을 다시 첨부해야 한다.
5. 테스트해야 할 항목
   - 직원 계정으로 사진·PDF 첨부 후 다른 PC 계정에서 열람
   - 사진 50KB 초과 및 문서·ZIP 10MB 초과 업로드 차단
   - 일반 계정의 타인 첨부 삭제 차단과 master 데이터 삭제 흐름

---

### 2026-08-14 첨부 자동 최저 압축 확장

1. 수정한 파일
   - `js/core.js`: 사진 압축 단계를 96px·품질 0.08까지 확대하고, 10MB 초과 문서·ZIP을 최고 압축 ZIP으로 자동 변환하는 처리 추가
   - `js/views-core.js`: 자동 사진·문서 압축 안내 문구 반영
   - `CLOUDFLARE_SUPABASE_구성안.md`: 실제 압축 기준 갱신
   - `index.html`, `js/app.js`, `js/core.js`, `js/views-core.js`, `js/views-ext.js`: 캐시버스트를 `20260814_autocompress1`로 일치 갱신
2. 수정한 기능
   - 사진은 저장 실패 대신 단계적 저해상도·저품질 WebP 변환을 계속 시도해 50KB 이하로 저장한다.
   - 10MB 초과 PDF·Office·한글·TXT·CSV·ZIP은 브라우저에서 최고 압축 ZIP으로 자동 변환한 후 저장한다.
3. 건드리지 않은 기능
   - R2 Worker의 최종 50KB/10MB 보안 제한, Supabase 데이터·권한·첨부 열람 구조는 변경하지 않았다.
4. 다음 작업자가 이어서 봐야 할 내용
   - PDF·이미지형 문서와 이미 압축된 ZIP은 최고 압축해도 10MB 이하가 되지 않을 수 있다. 이 경우에는 파일 형식 특성상 자동 압축만으로 해결할 수 없으며 앱이 저장을 차단한다.
5. 테스트해야 할 항목
   - 고용량 사진의 자동 WebP 변환 후 50KB 이하 R2 저장
   - 10MB 초과 Office·한글·ZIP의 자동 ZIP 변환, 변환 후 다른 계정 열람
   - 자동 압축 후에도 10MB를 넘는 PDF·ZIP의 명확한 안내 문구

---

### 2026-08-14 첨부 품질 보호·15MB·R2 3GB 저장량 관리 반영

1. 수정한 파일
   - `js/core.js`, `js/views-core.js`, `js/views-ext.js`: 사진 품질 보호 압축, 15MB 첨부 제한, R2 사용량 표시·백업 현황 반영
   - `cloudflare/attachment-worker/src/index.js`, `cloudflare/attachment-worker/wrangler.toml.example`, `cloudflare/attachment-worker/README.md`: R2 전역 3GB 한도와 사용량 조회 API 추가
   - `CLOUDFLARE_SUPABASE_구성안.md`, `index.html`, `js/app.js`: 운영 기준과 캐시버스트 갱신
2. 수정한 기능
   - 사진은 50KB를 목표로 WebP 압축하되 최소 긴 변 560px·품질 0.42 아래로 낮추지 않는다. 목표를 넘더라도 가장 작은 품질 보호본을 저장한다.
   - 사진·문서·ZIP 모두 변환 후 15MB 이하까지 저장한다. 15MB를 초과한 문서·ZIP은 최고 압축 ZIP 변환을 시도하고, 변환 후에도 15MB를 넘는 경우에만 저장을 차단한다.
   - Cloudflare R2 첨부 원본의 전역 저장량을 Worker에서 관리한다. 90%에서 경고하고 3GB 도달 시 새 파일 저장을 서버에서 차단한다.
   - 설정·백업 화면에서 현재 R2 사용량을 확인하고, 전체 백업 JSON에 첨부 메타데이터와 R2 사용량 현황을 포함한다. 원본 파일 자체는 R2에 보관된다.
3. 건드리지 않은 기능
   - Supabase 로그인·계정·권한·감사 이력, 법령·ISO 데이터, 이행기록과 첨부 메타데이터 구조, 기존 화면 구성은 변경하지 않았다.
4. 다음 작업자가 이어서 봐야 할 내용
   - R2 사용량 집계는 이 Worker 배포 이후 R2에 새로 저장·삭제되는 파일을 기준으로 한다. 기존 버킷에 별도로 넣은 파일이 있다면 최초 1회 실제 버킷 용량과 집계값을 맞추는 작업이 필요하다.
   - 전체 백업 JSON은 데이터·첨부 목록·R2 사용량 현황 백업이다. 최대 3GB 원본 파일 전체를 한 번에 내려받는 기능은 포함하지 않았다.
5. 테스트해야 할 항목
   - 고해상도 사진이 50KB를 넘더라도 560px·품질 0.42 이상 품질 보호본으로 저장되는지
   - 15MB 초과 Office·한글·TXT·CSV 파일의 ZIP 변환 후 저장, 변환 후 15MB 초과 시 차단 안내
   - 설정·백업 화면의 R2 사용량 표시, 파일 업로드 후 증가·삭제 후 감소, 90% 경고 및 3GB 차단

---

### 2026-08-18 외부 검토 보고서(GPT·Opus5) 대조 검증 및 보강

1. 배경
   - 사용자가 외부에서 GPT와 Claude Opus 5에게 받은 소스코드 기반 검토 보고서 2건(`아성다이소_SHMS_상세검토_및_보강권고안_20260818.docx`, `성다이소 SHMS 소스코드 기반 기술 검토 보고서.txt`)을 실제 배포 소스와 대조 검증하도록 요청.
   - 두 보고서의 핵심 지적(가입 시 role=safety 자동부여, 위험집계에서 '이행중' 누락, esm.sh CDN 의존, 이미지 49KB/560px/0.42 압축으로 인한 판독성·EXIF 손실, 3년 주기 누락, 반기 과거방향 생성, companyStatus 하드코딩, 완료판정 2단계 검증 부재, 조문 표기 오류)를 코드 라인 단위로 전부 확인함.
   - 검증 중 두 보고서 모두 언급하지 않은 결함을 추가 발견: 문서체계 6곳(`SHI-03,05,06,07,10` 및 안전기준수립 문서)이 존재하지 않는 `OSHA-038` id를 `lawRefs`로 참조하고 있었음(실제로는 `OSHA-037`이 법 제37~38조를 통합한 유일한 항목).
   - 사용자 지시: "로그인 문제는 건들지 말고 나머지를 진행" — 가입권한(P0-1)은 이번 작업에서 제외.
2. 수정한 파일
   - `js/views-core.js`: `renderDashboard()`의 `critical` 필터를 `['none','hold']`에서 `!['done','na'].includes(status)`로 변경(이행중도 위험으로 집계) + 위험 신호 안내문구 보강
   - `js/data/frameworks.js`: 존재하지 않는 `OSHA-038` 참조 6곳 → `OSHA-037` 정정 / `CYCLES`에 `3년 1회` 추가 / 근골격계 유해요인조사(OSHA-039A) 주기 연1회→3년1회, 조문 표기 시행규칙→안전보건규칙 정정 / 밀폐공간(OSHA-618) 조문 표기 동일 정정 / 직무스트레스(OSHA-039B) 근거에 안전보건규칙 제669조 추가 / 안전보건교육(OSHA-029) '분기 3시간'(폐지된 기준) → '반기 6/12시간' 현행 기준 및 주기 분기→반기 정정 / 화학물질 경고표시 항목의 'MDSD' 오탈자 → 'MSDS' 정정
   - `index.html`, `js/app.js`, `js/core.js`, `js/views-ext.js`: 캐시버스트 `20260814_capacity1` → `20260818_review1`
3. 건드리지 않은 기능 (사용자 지시로 제외)
   - 신규 가입 시 기본 role=safety 자동부여 문제 — 가입 즉시 전체 이행기록 편집이 가능한 구조가 그대로 남아있음. `supabase/schema.sql`의 `role text not null default 'safety'`와 `js/app.js`의 로그인 화면 안내문이 근거.
   - Supabase SDK esm.sh CDN 런타임 로드 — 로그인/인증 초기화와 직결되어 이번 범위에서 제외.
   - 이미지 압축(49KB/560px/0.42, EXIF 소실), 완료 판정 2단계 검증(검토대기/검증완료) 미도입, companyStatus 하드코딩·반기 미연동, 회원가입 정책은 코드 구조 변경 없이 보고서 내용만 검증함 — 별도 착수 필요.
4. 다음 작업자가 이어서 봐야 할 내용
   - 위 "건드리지 않은 기능" 항목이 여전히 P0/P1 미해결 상태로 남아있음. 특히 가입권한 문제는 사용자가 별도로 다루겠다는 의사를 표시했으므로, 먼저 확인 후 진행할 것.
   - `analyticsReportHtml()`의 `identified`(미흡사항 목록, `['none','hold']` 기준)는 이번 수정 대상에서 제외했음 — 위험 배지(`critical`)와는 별개 로직이라 의도적으로 남겨둠. 필요 시 통일 여부 재검토.
   - 폭염 항목(OSHA-039C)의 "본사·매장 해당 없음" 일괄 판정은 2025-07-17 개정(체감온도 31도 기준, 실내외 불문)과 배치될 수 있다는 보고서 지적이 있었으나, 이번 작업에서는 데이터 판정을 임의로 바꾸지 않음 — 실제 현장 온습도 측정 근거 확인 후 사용자가 판단할 사항.
5. 테스트한 항목 / 결과
   - `node --check` 전체 파일 통과
   - `_tools\github_deploy.ps1 -CheckOnly` 캐시버스트 일치·버전 변경 검사 통과
   - 배포 후 운영 URL(`https://sky5224122-jpg.github.io/daiso-shms/`)에서 `index.html` 캐시버스트 `20260818_review1` 확인, `frameworks.js`에서 `OSHA-038` 부재·`3년 1회` 존재·`MDSD` 부재·`분기 3시간` 부재를 fetch로 직접 확인
   - 화면상에서 위험집계('이행중' 포함) 동작은 브라우저로 직접 클릭 검증하지 않음 — 확인하지 못함

### 2026-08-18 시스템 전반 재검토 및 폭염 항목 매장 해당 반영

- 수정한 파일:
  - `js/data/frameworks.js`: OSHA-039C 폭염·한랭 항목 — companyStatus를 "매장 해당"으로 변경(기존 "본사·매장 해당없음" 삭제), requirement에 체감온도 31°C 이상 조치 의무(2025-07-17 개정) 추가, requiredDocs에 매장 관련 문서 추가
  - `js/core.js`: recentHalves() 함수에 다음 반기 1개 추가 — 반기 선택 드롭다운에서 미래 반기를 선택해 계획·이행기록 사전 수립 가능
  - `index.html`, `js/app.js`, `js/core.js`, `js/views-core.js`, `js/views-ext.js`: 캐시버스트 `20260818_review1` → `20260818_review2`
- 수정한 기능:
  - 폭염 항목 매장 적용 여부 판정 변경
  - 반기 선택기 미래 반기 포함
- 건드리지 않은 기능:
  - 로그인/가입 관련 (사용자 지시)
  - frameworks.js 조항 id (PK 보호)
  - 기존 이행기록 데이터
  - 이미지 EXIF 소실, STATUS 2단계 검증, companyStatus DB 분리 등 이전 세션 미해결 항목
- 전체 재검토 결과:
  - 중처법 14개, 산안법 28개, ISO 45001 25개 조항 + 문서체계 46건 — 유통·물류업 평가에 필요한 핵심 조항 전수 등록 확인
  - 캐시버스트 5개 파일 일치 확인
  - 조항 id 무결성 확인 (PK 변경 없음)
  - OSHA-038 참조 끊김 이전 수정(→OSHA-037) 유지 확인
  - 위험집계 critical 필터 ('done','na' 제외) 이전 수정 유지 확인
  - 법령 조문·주기 데이터(CYCLES, 안전보건규칙 조문, 교육시간) 이전 정정 유지 확인
- 테스트한 항목 / 결과:
  - `node --check` 전체 파일 통과
  - 배포 후 운영 URL에서 core.js 캐시버스트 `20260818_review2` 확인, recentHalves 다음 반기 포함 코드 확인
  - frameworks.js는 WebFetch 캐시로 인해 직접 조회 불가 — git diff로 변경 내용 검증 완료
- 확인하지 못한 것:
  - 실제 브라우저에서 반기 선택 드롭다운에 "2027년 상반기"가 표시되는지 클릭 확인하지 않음
- 다음 작업자가 봐야 할 것:
  - 여전히 미해결: 이미지 EXIF 소실, 완료 판정 2단계 검증, companyStatus DB 분리, Supabase CDN 의존
  - 매장 폭염 대응계획 실물 문서 작성 여부는 현장 확인 필요

### 2026-08-21 최신 법규 반영·평가 활용도 점검 (코드 수정 없음, 데이터 전수 확인)

- 수정한 파일: 없음 (사용자 질의에 대한 검증·보고만 수행, 코드 변경 없음)
- 확인한 내용:
  - 사용자 질문: "최신 법규에 맞게 구성되어 있는지 / 고용노동부 산안법 점검 및 중대재해 대응 방안으로 내용이 충실한지 / 도출·적용 내용이 제대로 반영되어 있는지"
  - `js/data/frameworks.js` 전체(767줄) 재확인 — MSSA 13개, OSHA 35개, ISO 45001 25개 = 73개 이행항목 및 문서체계 46건(DOC_MASTER) 전수 대조
  - 최신 개정사항 반영 확인: 중처법 5인 이상 확대(2024.01.27), 폭염 체감온도 31°C 기준(2025-07-17 개정, OSHA-039C), 산안법 제14조 이사회 보고, 고객응대근로자 보호(제41조), 휴게시설(제128조의2), 위험성평가 고시 개정(2023) 등
  - 고용노동부 점검 대응 항목(선임·위험성평가·교육·산안위·MSDS·고객응대·도급인 의무·건강진단·근골격계·밀폐공간·석면·안전보건표지·서류보존) 전부 등록 확인
  - 중대재해 발생 대응 흐름(작업중지→구호→보고→사고조사→CAPA 재발방지) 관련 항목·문서(OSHA-051/054, SHP-13/18, SHF-06/07, ISO-10.2) 연결 확인
- 발견한 문제 (미해결, 데이터 공백):
  - `companyStatus`(회사 이행현황)가 빈 문자열인 항목 8건 발견 — 실제 이행 여부가 시스템에 반영되지 않은 상태:
    - MSSA-4-9 (도급·용역 관리 기준·점검)
    - MSSA-5-4 (교육 미실시 확인 시 조치)
    - OSHA-042 (유해위험방지계획서)
    - OSHA-084 (안전인증 대상 기계·기구)
    - OSHA-119 (석면조사)
    - OSHA-125 (작업환경측정)
    - OSHA-128 (휴게시설 설치·운영)
    - OSHA-164 (서류의 보존)
  - 이 항목들은 "해당없음" 판정인지 "실제 미이행"인지 구분되지 않아, 심사·점검 대응 시 답변 공백이 생길 수 있음
- 사용자에게 보고한 결론: 법령 커버리지·문서체계·대응 흐름 구조는 충실하나, 위 8건은 현장 확인 후 companyStatus를 채워야 완전한 평가 도구가 됨
- 다음 작업자가 봐야 할 것:
  - 위 8건의 companyStatus 현장 확인 후 입력 (사용자 또는 안전보건팀 확인 필요 — AI가 임의로 "해당없음"으로 채우면 안 됨)
  - 과태료 금액 등 법조문 세부사항은 반기마다 국가법령정보센터 원문 대조 권장

---

### 2026-08-21 Claude 인수인계 대조·최신 운영 상태 정리

1. 수정한 파일
   - `작업인수인계.md`: Claude가 남긴 2026-08-21 법규·평가 점검 기록을 현행 코드·배포 상태와 대조한 최신 운영 요약 추가
   - `CLAUDE.md`: 과거 공용 비밀번호·Supabase 미연결 전제의 인증 안내를 현재 Supabase 계정 로그인·RLS·R2 권한 구조로 정정
   - `_context_handoff_20260813_1200.md`: 2026-08-13 과거 기록임을 상단에 명시
2. 수정한 기능
   - 기능 코드는 수정하지 않았다. 문서상 운영 기준만 최신화했다.
   - 현재 운영 기준: Supabase Auth 계정 로그인, 로그인한 사용자의 공통 조회, `master|safety|head` 작성·수정, `master`만 삭제, 첨부 원본은 Cloudflare R2 저장이다.
   - 사진은 50KB를 목표로 품질 보호 압축 후 저장하며, 사진·문서·ZIP 최종 제한은 15MB이다. R2 전체 사용량은 90% 경고·3GB 저장 차단 기준이다.
3. 건드리지 않은 기능
   - 법령·ISO 마스터 항목 id, 2026-08-18 법규 보강 내용, 이행기록·문서·점검·CAPA·첨부 데이터, Supabase/R2 실제 설정과 사용자 계정은 변경하지 않았다.
4. 다음 작업자가 이어서 봐야 할 내용
   - 최신 운영 배포 캐시버스트는 `20260818_review2`이다. 소스를 수정하면 `index.html`과 모든 상대 import의 `?v=` 값을 반드시 함께 올린다.
   - Claude의 8월 21일 확인 결과 중 실제 회사 판단이 필요한 `companyStatus` 공란 8건은 여전히 미입력 상태다. AI가 임의로 이행·해당없음 판정을 넣지 않는다.
   - 신규 계정 만들기는 현재 신규 사용자를 `safety` 역할로 생성한다. 신규 가입자의 작성 권한을 제한하려면 사용자 승인 후 운영 DB RLS와 스키마를 함께 변경해야 한다.
   - 전체 백업 JSON에는 업무 데이터·첨부 메타데이터·R2 사용량 현황이 포함된다. R2 원본 파일 전체를 하나의 JSON 또는 ZIP으로 내려받는 기능은 포함하지 않는다.
5. 테스트한 항목 / 결과
   - `_tools\\github_deploy.ps1 -CheckOnly`: `app.js`, `core.js`, `views-core.js`, `views-ext.js`, `frameworks.js`, `config.js` 문법 검사와 캐시버스트 일치 검사 통과
   - 운영 주소에서 `20260818_review2` 캐시버스트 반영 확인
   - 운영 `core.js`에서 Supabase 계정 로그인 함수와 15MB 첨부 제한 반영 확인
   - R2 Worker `/usage`에 인증 없이 접근 시 HTTP 401 차단 확인

---

### 2026-08-24 로그인 아이디 클릭 시 최근 사용 아이디 선택 기능 추가

1. 수정한 파일
   - `js/app.js`: 로그인 화면 아이디 입력칸(`#lgLoginId`)에 `list="lgLoginIdList"` 속성과 `<datalist id="lgLoginIdList">` 추가. 최근 로그인 아이디를 `localStorage`(`shms_recent_login_ids` 키, 최대 8개, 중복 제거·최신순)에 저장하는 `getRecentLoginIds()`/`saveRecentLoginId()` 헬퍼 추가. 로그인 성공 직후 입력한 아이디를 저장하도록 제출 핸들러에 1줄 추가
   - `index.html`, `js/core.js`, `js/views-core.js`, `js/views-ext.js`: 캐시버스트 `20260818_review2` → `20260824_loginid` (로직 변경 없음, 값 동기화만)
2. 수정한 기능
   - 로그인 화면에서 아이디 입력칸을 클릭/포커스하면 이 브라우저에서 최근 로그인했던 아이디 목록(최대 8개)이 브라우저 네이티브 드롭다운으로 뜨고 클릭 선택 가능
3. 건드리지 않은 기능
   - `signIn`/`signUp` 등 로그인 인증 로직 자체는 수정하지 않음 (이전 세션 지시 "로그인 문제는 건들지 말 것" 준수 — 이번 요청은 인증 로직이 아닌 UI 편의 기능 추가라 별개로 판단)
   - 비밀번호 필드, 계정 만들기 로직 변경 없음
4. 테스트한 항목 / 결과
   - `node --check` 전체 5개 파일 통과
   - 로컬 미리보기(`http://localhost:8899`)에서 `localStorage`에 테스트 아이디 3개 주입 후 재로딩 → `input[list]`와 `datalist`의 id 매칭, `datalist` 옵션에 3개 아이디 정상 반영 확인 (JS로 DOM 상태 직접 확인, 네이티브 드롭다운 UI 자체는 OS 렌더링이라 스크린샷으로는 확인 불가 — HTML5 표준 동작이므로 브라우저에서 정상 작동 예상)
   - 커밋(`ce135e1`) 및 원격 push 완료
5. 확인하지 못한 것
   - 실제 사용자 브라우저(Chrome/Edge 등)에서 클릭 시 드롭다운이 시각적으로 뜨는지는 사람이 직접 클릭해서 눈으로 확인 필요 (자동화 도구로는 OS 네이티브 드롭다운 렌더링을 캡처할 수 없음)
6. 다음 작업자가 봐야 할 것
   - 이 기능은 각 사용자의 브라우저(기기)별로 개별 저장되며 서버·DB에 저장되지 않음 — 다른 기기·브라우저에서는 최근 아이디 목록이 뜨지 않는 것이 정상
   - 최신 캐시버스트는 `20260824_loginid`

---

### 2026-08-24 접속 관문(guest01~03) 추가 — 비인가자 접근 차단

- 배경: 사용자가 처음엔 "게스트01~03 / 비밀번호 c123456 계정을 만들어달라"고 요청. Supabase 회원가입(signUp)을 실제로 시도했으나 `Email address "guest01@accounts.daiso-shms.local" is invalid` 오류로 **가입 자체가 Supabase 서버 단에서 거부됨**(도메인 검증 강화로 추정, 원인 미확정). 이후 사용자가 "이건 정말 아이디를 만드는게 아니라 접속 시 아무나 접속하지 못하게 막는 게 목적"이라고 의도를 정정 → 실제 Supabase 계정 생성이 아니라 **로그인 화면 이전 단계의 차단막**으로 재설계
- 수정한 파일:
  - `js/app.js`: `renderGate()` 함수 신규 추가 — 실제 로그인 화면 이전에 뜨는 접속 코드 확인 화면. 아이디(`guest01`/`guest02`/`guest03`) + 코드(`c123456`, SHA-256 해시로만 소스에 저장)를 맞게 입력해야 통과. 통과 시 `sessionStorage`에 `shms.gatepass` 플래그 저장(같은 탭 세션 동안 재확인 안 함). `boot()` 함수에서 `state.user`가 없을 때 이 플래그가 없으면 `renderGate()`를 먼저 띄우도록 연결
  - `index.html`, `js/core.js`, `js/views-core.js`, `js/views-ext.js`: 캐시버스트 `20260824_loginid` → `20260824_gate` (로직 변경 없음, 값 동기화만)
- 수정한 기능:
  - URL만 아는 비인가자가 실제 로그인 화면(및 "계정 만들기" 자동가입 버튼)에 아예 도달하지 못하도록 1차 관문 추가
  - 관문 통과는 시스템 로그인이 아니며, 통과 후에도 실제 이용자는 본인의 진짜 Supabase 계정(아이디·비밀번호)으로 다시 로그인해야 함 — 관문 코드 자체는 어떤 role·권한도 부여하지 않음
- 건드리지 않은 기능:
  - `signIn`/`signUp` 등 실제 Supabase 인증 로직, RLS, `shms_profiles` 테이블 — 전혀 손대지 않음
  - Supabase 프로젝트의 이메일 도메인 검증 설정(회원가입 실패의 근본 원인)은 접근 권한이 없어 확인·수정하지 못함. 필요 시 Supabase 대시보드 Authentication 설정에서 직접 확인 필요
  - 게스트 계정 3개는 결국 생성하지 않음 (사용자가 방향을 "차단막"으로 정정했기 때문)
- 테스트한 항목 / 결과:
  - `node --check` 전체 5개 파일 통과
  - 로컬 미리보기에서: (1) 틀린 코드 입력 시 "접속 코드가 올바르지 않습니다" 오류로 차단 확인 (2) `guest02`/`c123456` 입력 시 정상 통과하여 실제 Supabase 로그인 화면으로 전환 확인 (3) 통과 후 페이지 새로고침해도 같은 탭에서는 관문이 다시 뜨지 않고 바로 로그인 화면으로 감을 확인(`sessionStorage` 세션 유지 정상)
  - 커밋(`f24160d`) 및 원격 push 완료
- 확인하지 못한 것:
  - 새 탭/새 방문자(세션스토리지 없음)에서 관문이 다시 뜨는지는 구조상 당연하나 별도 탭으로 직접 재확인은 하지 않음
  - Supabase 회원가입이 `.local` 도메인 때문에 막힌 근본 원인(도메인 화이트리스트 설정 여부 등)은 대시보드 접근 권한이 없어 미확인 — 향후 실제 회원가입 기능이 필요해지면 이 부분부터 확인해야 함
- 다음 작업자가 봐야 할 것:
  - 접속 코드를 바꾸려면 `js/app.js`의 `GATE_CODE_HASH`를 새 해시로 교체 (`node -e "console.log(require('crypto').createHash('sha256').update('새코드','utf8').digest('hex'))"`)
  - 관문 통과 여부는 세션스토리지 기반이라 브라우저 탭을 닫으면 초기화됨(의도된 동작)
  - 최신 캐시버스트는 `20260824_gate`

---

### 2026-08-24 게스트 읽기전용 열람 + 설정/백업/복원 분리 및 역할별 접근 제한

- 배경: 사용자가 이어서 4가지를 요청 — (1) 게스트로 접속하면 확인만 가능 (2) 마스터로 접속하면 설정에 전체 아이디 표시 (3) 마스터가 아니면 설정/백업에 접속 불가 (4) 설정·백업·복원 분리, 백업은 안전보건팀 다른 직원도 다운로드 가능
- 수정한 파일:
  - `js/app.js`:
    - 접속 관문 화면에 "게스트로 열람(읽기 전용)" 버튼 추가. `guest01~03`/`c123456` 코드 확인 후 Supabase 계정 없이 로컬 전용 `GUEST_USER`(role:`guest`) 세션을 만들어 바로 앱으로 진입시킨다. `sessionStorage`(`shms.guestsession`)로 새로고침에도 세션 유지, 로그아웃 시 해제
    - `NAV` 배열의 "시스템" 그룹을 `설정`(roles:['master']) / `백업`(roles:['master','safety','head']) / `복원`(roles:['master']) 3개 항목으로 분리
    - `renderNav()`에 역할 필터링 추가 — 권한 없는 메뉴는 사이드바에서 아예 숨김(그룹 전체가 비면 그룹도 숨김)
    - `currentView()`에 역할 검증 추가 — 권한 없는 화면으로 URL 해시를 직접 바꿔도 대시보드로 자동 전환(우회 차단)
    - `route()`에 `backup`/`restore` case 추가
  - `js/views-ext.js`:
    - 기존 `renderSettings()`/`bindSettingsEvents()`에서 백업·복원 카드를 분리하고, 새로 "🆔 전체 사용자 계정" 카드 추가 — `shms_profiles` 테이블에서 로그인 아이디·이름·부서·권한·가입일 전체 조회(기존 RLS 정책 `shms_profiles_self_read`가 master에게 전체 조회를 이미 허용하고 있어 schema.sql 변경 없이 구현됨)
    - `renderBackup()`/`bindBackupEvents()` 신규 — 전체 백업 다운로드 버튼 + 자동 백업 현황(조회만, 복원/삭제 버튼 없음)
    - `renderRestore()`/`bindRestoreEvents()` 신규 — 백업 파일 복원 + 자동 백업 시점 복원/삭제(마스터 전용 경고 배너 포함)
  - `js/data/frameworks.js`: `ROLES`에 `guest`(게스트, 읽기 전용) 항목 추가(표시 라벨용, 조항 id는 변경하지 않음)
  - `index.html`, `js/core.js`, `js/views-core.js`: 캐시버스트 `20260824_gate` → `20260824_roles`
- 수정한 기능:
  - 게스트 접속 = 실제 계정 없이 읽기 전용으로 전체 화면 열람 가능(작성·수정·삭제 버튼은 기존 `canEdit()`/`canDelete()` 로직에 의해 자동으로 숨겨짐 — 이 로직은 이번에 새로 만든 것이 아니라 기존에 모든 화면(문서·점검·CAPA·증빙·조직·이행기록 드로어)에 이미 적용되어 있던 것을 그대로 활용)
  - 설정 화면은 마스터만 접근 가능, 그 안에 전체 계정 목록 표시
  - 백업/복원을 별도 화면으로 분리, 백업은 안전보건팀(master·safety·head) 전원 접근 가능
- 건드리지 않은 기능:
  - `signIn`/`signUp` 로그인 인증 로직, Supabase RLS 정책(schema.sql은 이번에 수정하지 않음 — 기존 정책으로 충분히 구현 가능했음)
  - MSSA/OSHA/ISO 조항 `id` 값 — 전혀 변경 없음
- ⚠️ 확인된 중요한 한계 (사용자에게 별도 안내함):
  - 게스트 세션은 실제 Supabase 인증 세션이 아니므로, RLS 정책(`to authenticated`)에 의해 **Supabase의 실시간 데이터를 읽어올 권한이 없다.** `loadAll()`의 기존 폴백 로직에 의해 이 브라우저에 이전에 캐시된 로컬 자료가 있으면 그것이 보이고, 없으면(완전히 새 기기) 빈 상태로 보인다 — 실제 브라우저 테스트에서 "미이행 13건"으로 빈 화면이 뜨는 것을 확인함
  - 이를 모든 기기에서 실시간 데이터가 보이도록 완전히 해결하려면 Supabase 대시보드에서 "Anonymous sign-ins"를 활성화해야 하는데, curl로 직접 확인한 결과 **현재 비활성화 상태**(`anonymous_provider_disabled`)다. 활성화 후에는 `shms_handle_new_user` 트리거가 익명 사용자에게도 기본 role='safety'(쓰기 권한)를 부여하므로, 트리거를 수정해 익명 사용자는 read-only role을 받도록 하는 schema.sql 변경이 추가로 필요하다 — 아직 하지 않음
- 테스트한 항목 / 결과:
  - `node --check` 전체 5개 파일 통과
  - 로컬 미리보기에서: (1) "게스트로 열람" 클릭 시 실제 로그인 없이 대시보드까지 정상 진입 확인 (2) 사이드바에 "시스템"(설정/백업/복원) 메뉴 그룹이 아예 보이지 않음을 확인 (3) `location.hash`를 `#/settings`로 강제 변경해도 대시보드 제목이 유지됨(우회 차단) 확인 (4) `canEdit()`가 문서·점검·CAPA·증빙·조직·드로어 등 전 화면에서 일관되게 사용되고 있음을 코드로 확인(게스트 role이 자동으로 읽기 전용 처리됨)
  - 커밋(`d43e8d5`) 및 원격 push 완료
  - 마스터 계정으로 실제 로그인해 설정/백업/복원 화면과 전체 계정 목록이 정상 표시되는지는 실제 마스터 계정이 없어 브라우저로 직접 클릭 검증하지 못함 — 코드 로직(역할 배열 필터링)만 확인
- 확인하지 못한 것:
  - 마스터 계정으로 실제 로그인 후 설정/백업/복원 3개 화면이 실제로 올바르게 나뉘어 보이는지, "전체 사용자 계정" 표가 실제 데이터로 정상 채워지는지 — 실제 마스터 로그인 테스트 필요
  - safety/head 역할 계정으로 로그인해 "백업" 메뉴만 보이고 "설정"/"복원"은 안 보이는지 실제 클릭 검증 필요
- 다음 작업자가 봐야 할 것:
  - 게스트가 모든 기기에서 실시간 데이터를 보게 하려면: ① Supabase 대시보드에서 Anonymous sign-ins 활성화 ② `supabase/schema.sql`의 `shms_handle_new_user` 트리거를 수정해 `new.is_anonymous`인 경우 role을 'safety'가 아닌 읽기전용 role로 넣도록 변경 — 두 가지 모두 사용자 승인 후 진행
  - 최신 캐시버스트는 `20260824_roles`
  - 다음 실제 로그인 시 마스터/safety/head 각 역할로 메뉴 노출이 의도대로 되는지 확인 권장

---

### 2026-08-24 (긴급) 접속 관문 제거 — 마스터 로그인 차단 문제 즉시 수정

- 문제 발생 경위: 위 커밋(`73f71f4`, `d43e8d5`)에서 추가한 "접속 관문" 화면이 **실제 로그인 화면보다 먼저 항상 뜨도록** 구현되어 있었다. 이 화면은 `guest01~03`/`c123456` 코드만 받는데, 생김새가 일반 로그인 화면과 비슷해서 사용자가 실제 마스터 아이디("master")와 비밀번호를 이 관문 화면에 입력 → "접속 코드가 올바르지 않습니다" 오류로 **본인(마스터)조차 로그인 불가** 상태가 됨. 사용자가 스크린샷으로 증상을 보내와 즉시 확인·수정
- 수정한 파일:
  - `js/app.js`:
    - `renderGate()` 함수와 관문 강제 진입 로직(`boot()`의 `GATE_PASS_KEY` 분기) 전체 삭제 — `!state.user`일 때 바로 `renderLogin()`(실제 로그인 화면)을 띄우도록 원복
    - "게스트로 열람" 기능은 삭제하지 않고 **실제 로그인 화면 안**으로 이동 — `renderLogin()` 하단에 "게스트로 열람 (읽기 전용)" 버튼 추가, 클릭 시 그 자리에서 게스트 아이디/코드 입력창이 펼쳐짐(별도 화면 전환 없음)
    - `checkGateCode()` → `checkGuestCode(idFieldId, codeFieldId)`로 일반화(필드 id를 인자로 받아 로그인 화면의 `guestId`/`guestCode`에서도 재사용)
    - `GATE_PASS_KEY` 상수 삭제(더 이상 필요 없음), `GATE_IDS`/`GATE_CODE_HASH` → `GUEST_IDS`/`GUEST_CODE_HASH`로 이름 정리(값은 동일, `guest01~03`/`c123456`)
  - `index.html`, `js/core.js`, `js/views-core.js`, `js/views-ext.js`: 캐시버스트 `20260824_roles` → `20260824_nogate`
- 수정한 기능:
  - 접속 시 실제 로그인 화면(아이디/비밀번호)이 다시 바로 뜬다 — 마스터를 포함한 모든 실제 계정 로그인이 정상 작동
  - 게스트 읽기 전용 열람 기능은 그대로 유지(로그인 화면 내 버튼 형태로)
- 건드리지 않은 기능:
  - 2026-08-24 앞선 커밋에서 만든 설정/백업/복원 분리, 역할별 메뉴 제한(`roles` 배열), `canEdit()`/`canDelete()` 로직 — 전혀 변경하지 않음
  - `signIn`/`signUp` 로그인 인증 로직 자체는 이번에도 손대지 않음
- 테스트한 항목 / 결과:
  - `node --check` 전체 5개 파일 통과
  - 로컬 미리보기에서: (1) 접속 시 관문 없이 바로 아이디/비밀번호 로그인 화면이 뜸을 확인 (2) "게스트로 열람" 클릭 → 인라인으로 게스트 아이디/코드 입력창이 나타남을 확인 (3) `guest01`/`c123456` 입력 후 "게스트로 입장" 클릭 → 대시보드까지 정상 진입 확인 (4) `sessionStorage` 초기화 후 새로고침해도 실제 로그인 화면이 정상적으로 다시 뜸을 확인
  - 커밋(`471afc1`) 및 원격 push 완료
- 확인하지 못한 것:
  - 실제 마스터 계정(진짜 비밀번호)으로 직접 로그인해 문제가 완전히 해결됐는지는 사용자가 실제로 로그인해봐야 최종 확인됨 — 배포 후 사용자 확인 필요
- 다음 작업자가 봐야 할 것:
  - "관문(모든 방문자를 막는 벽)"과 "게스트 읽기전용 로그인"은 서로 다른 개념이다. 전자는 실제 계정 로그인 자체를 막아 부작용이 크므로 재도입하지 않는다. 후자(게스트 버튼)만으로 "아무나 자료를 작성·수정하지 못하게" 하는 목적은 이미 충족된다
  - 최신 캐시버스트는 `20260824_nogate`

---

### 2026-09-04 ISO45001 전 조항(30개) 실행문서 이행평가 결과 반영
- 수정한 파일:
  - `js/data/frameworks.js`: ISO_ITEMS 30개 항목에 `companyStatus` 필드 추가
  - `index.html`, `js/app.js`, `js/core.js`, `js/views-core.js`, `js/views-ext.js`: 캐시버스트 `20260903_doc44` → `20260904_iso`
- 수정한 기능:
  - ISO 45001 조항 4.1~10.3 전 항목에 `[26H1 실행문서 평가]` 이행상태 및 판정(●적정/▲보완/✕미흡) 등록
  - 데이터 출처: `D:\☆ 중대재해처벌법 대응\1. 실행 내역\1. 절차서 관련 실행문서\26년\26년 절차서 관련 실행문서 현황표_26_0831.xlsx` + `13.2 하반기 업무수행 평가기준표_안전보건관리책임자_26_0706(원본파일수정).docx`
  - 판정 분포: ●적정 7건(5.1, 8.1.1, 9.1.2, 10.1, 10.2, 10.3) / ▲보완 21건 / ✕미흡 2건(7.5 문서관리, 8.1.3 변경관리)
  - ISO 심사 결과 보정: 13.2 문서의 fabricated 심사결과를 '6~7월 예정, 실제 심사일자·결과 확인 필요'로 통일 반영(ISO-9.2 companyStatus)
- 건드리지 않은 기능:
  - MSSA_ITEMS, OSHA_ITEMS의 기존 companyStatus — 변경 없음
  - DOC_MASTER 문서번호 매핑 — 변경 없음
  - 인증/권한/화면 로직 — 변경 없음
- 테스트한 항목 / 결과:
  - `node --check` 전체 5개 JS 파일 통과
  - Grep 확인: `companyStatus.*실행문서` 30건 정확히 일치
- 확인하지 못한 것:
  - 운영 URL에서 ISO 항목 카드에 companyStatus가 실제로 표시되는지 — 배포 후 확인 필요
- 다음 작업자가 봐야 할 것:
  - 최신 캐시버스트는 `20260904_iso`
  - ISO-9.2(내부심사) companyStatus에 '실제 심사일자·결과 확인 필요'로 기재 — 심사 완료 시 갱신 필요
  - ✕미흡 2건(ISO-7.5 문서관리, ISO-8.1.3 변경관리)은 실행문서 마련 후 갱신 필요

---

### 2026-08-25 메모장 신규 추가 + ISO 요구사항 관리에 심사 개요 추가

- 사용자 요청: (1) 심사결과·내용을 메모할 수 있는 메모장 (2) "요구사항 관리"(ISO) 화면에 심사실시일·심사자·심사내용·심사결과를 넣는 "심사 개요" 구성
- 수정한 파일:
  - `js/core.js`: `TABLES`에 `memos`(shms_memos), `auditOverview`(shms_audit_overview) 추가. `state`에 `memos:[]`, `auditOverview:[]` 추가. `loadAll()`에서 두 테이블을 함께 조회하되, 아직 Supabase에 테이블이 없어도(schema.sql 미실행 상태) 나머지 데이터 로딩이 깨지지 않도록 이 두 테이블의 조회 오류는 별도로 무시 처리. `persistAll()`에 두 항목 로컬 저장 추가. 기존의 범용 `saveRow(kind,row)`/`deleteRow(kind,id)` 메커니즘을 그대로 재사용해서 새 함수를 따로 만들지 않음
  - `js/views-ext.js`: "11. 메모장" 섹션 신규 — `renderMemo()`/`bindMemoEvents()`, 내부적으로 `openMemoDrawer()`가 기존 CAPA/증빙과 동일한 `openDrawer()` 패턴 사용. 필드: 일자·구분(심사/점검/회의/전화·구두 지시/기타)·제목(선택)·내용
  - `js/views-core.js`: `renderCompliance(fw)`에서 `fw==='iso'`일 때만 `auditOverviewSectionHtml()`(심사 개요 카드)를 배너 바로 아래 삽입. `openAuditOverviewDrawer()`(심사실시일·심사자·심사결과[적합/경미 부적합/중대 부적합/관찰사항]·심사내용) 추가. `bindComplianceEvents()`에 심사개요 등록/행클릭/삭제 이벤트 바인딩 추가. `saveRow`/`deleteRow`/`today`를 core.js에서 새로 import
  - `js/app.js`: NAV "실행 관리" 그룹에 `memo`(메모장) 항목 추가, `renderMemo`/`bindMemoEvents` import 및 `route()`에 `case 'memo'` 추가
  - `supabase/schema.sql`: `shms_memos`, `shms_audit_overview` 테이블 정의 추가. 기존 RLS 정책 자동생성 루프의 배열에 두 테이블명을 추가해서 별도 정책 작성 없이 기존과 동일한 권한 규칙(조회: 로그인 전체, 작성/수정: master·safety·head, 삭제: master)이 자동 적용되도록 함
  - `index.html`, `js/app.js`, `js/core.js`, `js/views-core.js`, `js/views-ext.js`: 캐시버스트 `20260824_nogate` → `20260825_memo`
- 수정한 기능:
  - 새 메뉴 "메모장" — 자유 텍스트 메모 작성·조회·삭제(삭제는 마스터만)
  - "요구사항 관리"(ISO) 화면 상단에 "🗒️ 심사 개요" 카드 — 여러 건 등록 가능한 표 형태(단일 개요가 아니라 이력 목록)
- 건드리지 않은 기능:
  - MSSA/OSHA 화면에는 심사 개요 카드가 뜨지 않음(ISO 전용으로 조건 분기, 요구사항 명시대로 구현)
  - 기존 이행기록·문서·점검·CAPA·증빙·조직 데이터 구조 변경 없음
- ⚠️ 사용자가 알아야 할 것:
  - **`supabase/schema.sql`의 새로 추가된 `shms_memos`, `shms_audit_overview` 테이블 정의를 아직 실제 Supabase 프로젝트에 반영하지 않았다.** Supabase 대시보드 → SQL Editor에서 schema.sql 전체(또는 새로 추가된 두 테이블 정의+RLS 루프 부분)를 다시 실행해야 여러 사용자 간에 메모·심사개요가 동기화된다
  - 테이블을 실행하기 전까지는 메모장·심사개요 모두 **이 브라우저에만(로컬)** 저장되며, 다른 사람/다른 기기에는 보이지 않는다 — 앱이 알아서 로컬 저장으로 정상 동작하지만 "공유"는 안 되는 상태
- 테스트한 항목 / 결과:
  - `node --check` 전체 5개 파일 통과
  - 로컬 미리보기에서 게스트로 접속해 메모장·요구사항 관리 화면이 오류 없이 렌더링되는지 확인
  - 브라우저 콘솔에서 core.js 모듈의 `state.user`를 임시로 master 역할로 지정해(실제 코드 변경 아님, 테스트 목적의 런타임 조작) 메모 작성→저장→목록 표시, 심사 개요 등록→저장→목록 표시, 심사 개요 삭제까지 전 과정 정상 동작 확인 후 테스트 데이터 정리(localStorage 초기화)
  - 커밋(`dee2b9b`) 및 원격 push 완료
- 확인하지 못한 것:
  - 실제 마스터/safety 계정으로 로그인해 같은 흐름을 클릭으로 확인하지 않음(테스트는 런타임 role 임시 조작으로 진행)
  - Supabase에 새 테이블을 실제로 만든 뒤 여러 브라우저 간 동기화가 되는지는 사용자가 SQL을 실행한 뒤에만 확인 가능
- 다음 작업자가 봐야 할 것:
  - **schema.sql을 Supabase SQL Editor에서 재실행해야 메모장·심사개요가 팀 전체에 공유된다** (사용자에게 안내 예정)
  - 최신 캐시버스트는 `20260825_memo`

### 2026-09-03 26년 상반기 이행평가 보고서 기반 허브센터 데이터 업데이트

- 수정한 파일:
  - `js/data/frameworks.js`: MSSA·OSHA 항목의 `companyStatus`와 `evidenceFiles` 업데이트
  - `index.html`, `js/app.js`, `js/core.js`, `js/views-core.js`, `js/views-ext.js`: 캐시버스트 `20260825_memo` → `20260903_hub`
- 수정한 기능:
  - 「26년 상반기 아성다이소 중처법 이행상태 점검·평가 통합 보고서」를 분석하여 허브센터(남사·부산·안성) 평가 결과를 시스템에 반영
  - **MSSA 항목 업데이트(10개 항목)**:
    - MSSA-4-1(목표·경영방침): 전체 ▲보완, 허브센터 팀별 정량목표 세분화 미흡, 개선과제 3건 등록
    - MSSA-4-2(전담조직): 본사 ▲보완, 물류허브센터 해당없음, 대표이사 직보체계 문서화 필요
    - MSSA-4-3(위험요인 확인·개선): 본사 ▲보완 / 3센터 ●적정, 매장 위험성평가 전산화 전환 필요
    - MSSA-4-4(예산 편성·집행): 전체 ▲보완, 예산편성 프로세스 부재, 26.08 안전보건공시 시행
    - MSSA-4-5(업무수행 평가): 전체 ●적정
    - MSSA-4-6(전문인력 배치): 전체 ●적정, 본사 지역별 별도 인력 배치 검토 권고
    - MSSA-4-7(종사자 의견 청취): 본사 ▲보완 / 3센터 ●적정, QR채널·플랫폼 구축 필요
    - MSSA-4-8(비상조치): 본사·남사 ▲보완 / 부산·안성 ●적정, 시나리오 다양화·작업중지권 교육
    - MSSA-4-9(도급·용역·위탁): 안성 ●적정 / 나머지 ▲보완, 도급 계약 시 안전보건 평가항목 명시 필요
    - MSSA-5-1(관계법령 의무이행): 전체 ●적정
    - MSSA-5-3(교육 실시 점검): 전체 ●적정, 허브센터 교육 서명지 증빙 추가
  - **OSHA 항목 업데이트(8개 항목)**:
    - OSHA-024(산안위): 허브센터 회의록 증빙 등록
    - OSHA-029(안전보건교육): 허브센터 교육 서명지·TBM 교안 증빙 등록
    - OSHA-036(위험성평가): 허브센터 정기·수시 위험성평가 결과보고서 등록
    - OSHA-037(안전조치): 허브센터 순찰·점검 보고서 증빙 등록
    - OSHA-039A(근골격계): 남사·부산 유해요인조사 결과보고 증빙 등록
    - OSHA-054(중대재해 조치): 전체 ●적정 반영, 사고보고서 증빙 등록
    - OSHA-062(도급 합동점검): 허브센터 협의체·합동점검·순회점검 증빙 등록
    - OSHA-080(유해위험기계 방호): 허브센터 설비대장·점검일지 증빙 등록
    - OSHA-093(안전검사): 남사·부산 안전검사 결과통지서·설비대장 증빙 등록
    - OSHA-110(MSDS): 남사·부산 화학물질 관리대장 증빙 등록
  - **보고서 개선과제 12건** 해당 항목 companyStatus에 [개선] 태그로 반영
  - `evidenceFiles`에 허브센터별 증빙자료를 `[남사]`, `[부산]`, `[안성]` 태그로 분류 등록
- 건드리지 않은 기능:
  - `frameworks.js` 조항 `id` 변경 없음 (기존 이행기록 호환 유지)
  - 본사·매장 전용 OSHA 항목(고객응대근로자, 휴게시설, 건강진단 등) 변경 없음
  - ISO 45001 항목 변경 없음
  - UI·라우팅·인증 로직 변경 없음
- 테스트한 항목 / 결과:
  - `node --check` 전체 5개 JS 파일 + frameworks.js 구문 오류 없음
  - 캐시버스트 5개 파일 모두 `20260903_hub` 동일 확인 (13건)
  - 커밋(`b915c3c`) 및 원격 push 완료
- 확인하지 못한 것:
  - 운영 사이트에서 각 항목 상세화면의 companyStatus·evidenceFiles가 정확히 표시되는지 육안 확인하지 않음
  - 보고서 PDF의 일부 내용(사업장 현황표 내 인원수 등)은 마스킹 처리되어 있어 정확한 수치 미반영 부분이 있을 수 있음
- 다음 작업자가 봐야 할 것:
  - 보고서 내 개선과제 12건의 완료예정일 대부분 2026년 하반기 — 하반기 평가 시 이행현황 갱신 필요
  - `evidenceFiles` 배열에 등록된 이름들은 실제 첨부 파일이 아닌 **참조용 텍스트** (첨부 기능으로 실제 PDF/Excel 업로드는 별도)

### 2026-09-03 산재현황 통계 반영 및 대시보드 앱 바로가기 추가
- 수정한 파일:
  - `js/data/frameworks.js`: OSHA-054(중대재해 조치·보고) companyStatus에 2026년 1~7월 산재승인 통계 추가, evidenceFiles에 산재현황 문서 참조 추가
  - `js/views-core.js`: renderDashboard() 함수 하단에 "관련 시스템 바로가기" 섹션 추가 (위험성평가 v1~v7, 비상대응훈련 앱 링크)
  - `index.html`, `js/app.js`, `js/core.js`, `js/views-core.js`, `js/views-ext.js`: 캐시버스트 `20260903_hub` → `20260903_stat` 변경
- 수정한 기능:
  - OSHA-054 항목에 26년 산재현황 데이터 반영: 전사 55건(사고47·출퇴근8), 근로손실일수 3,854일, 허브센터 0건, 25년 대비 참고치
  - 대시보드 맨 아래에 위험성평가(v1 마스터~v7) 7개 + 비상대응훈련 1개 = 8개 앱 바로가기 카드 링크 추가 (새 탭으로 열림)
- 건드리지 않은 기능:
  - frameworks.js 조항 id 변경 없음
  - 로그인·인증·권한 로직 변경 없음
  - 기존 대시보드 KPI·보고서·목록 기능 변경 없음
- 테스트한 항목 / 결과:
  - `node --check` 5개 JS 파일 구문 오류 없음
  - 캐시버스트 5개 파일 모두 `20260903_stat` 동일 확인 (13건)
  - 커밋(`818a909`) 및 원격 push 완료
- 확인하지 못한 것:
  - 운영 사이트에서 대시보드 하단 앱 링크가 정상 렌더링되는지 육안 확인하지 않음
  - 각 위험성평가 앱(v1~v7) 및 비상대응훈련 앱 URL의 실제 접속 가능 여부 미확인
- 다음 작업자가 봐야 할 것:
  - 산재현황 데이터는 26년 8월 기준 승인분까지 반영. 하반기 데이터 갱신 필요
  - 실명은 제외하고 집계 통계만 소스에 반영됨 (개인정보 미포함)
  - 최신 캐시버스트는 `20260903_doc44`

### 2026-09-04 ISO45001 하반기 최종문서 44종 + 안전보건관리규정 등록
- 수정한 파일:
  - `js/data/frameworks.js`: DOC_MASTER에 총 18개 항목 추가 (SHM-01 규정 1건, SHP-22~23 절차서 2건, SHI-13~27 지침서 15건) + 기존 26개 항목에 companyDocNo(회사 문서번호) 매핑
  - `js/core.js`: seedDocuments()에 company_doc_no 필드 추가
  - `js/views-ext.js`: 문서 카드에 회사 문서번호 표시, 상세 드로어에 "회사 문서번호" 입력 필드 추가, 저장 로직에 company_doc_no 포함
  - 5개 파일 캐시버스트 `20260903_stat` → `20260903_doc44`
- 수정한 기능:
  - 안전보건관리규정 AAD-HSHT-M-2021-002(7)을 SHM-01로 매뉴얼 등록
  - ISO45001 하반기 최종문서 44종의 회사 문서번호(AAD-HSHT-*)를 SHMS 문서번호(SHP/SHI)에 매핑
  - 문서번호 매핑 예시: SHP-05 ↔ AAD-HSHT-P-2022-001(4) 위험성평가, SHP-13 ↔ AAD-HSHT-P-2022-011(6) 비상대응 등
  - 문서 카드 및 드로어에서 회사 문서번호 확인·편집 가능
- 건드리지 않은 기능:
  - frameworks.js 조항 id 변경 없음 (MSSA/OSHA/ISO 이행기록 호환 유지)
  - 기존 SHF(양식) 항목 변경 없음
  - 로그인·인증·권한 로직 변경 없음
  - Supabase 스키마 변경 없음 (company_doc_no는 클라이언트 측 필드)
- 테스트한 항목 / 결과:
  - `node --check` 5개 JS 파일 구문 오류 없음
  - 캐시버스트 5개 파일 모두 `20260903_doc44` 동일 확인 (13건)
  - 커밋(`a1b9cc8`) 및 원격 push 완료
- 확인하지 못한 것:
  - 운영 사이트에서 새 문서 항목(SHM-01, SHP-22~23, SHI-13~27)이 목록에 정상 표시되는지 육안 미확인
  - 기존 사용자의 localStorage 기반 문서 데이터와 새 seed 항목 병합이 정상 동작하는지 실제 환경 미확인
  - Supabase documents 테이블에 company_doc_no 컬럼이 없으므로 서버 동기화 시 해당 값이 보존되는지 확인 필요
- 다음 작업자가 봐야 할 것:
  - Supabase shms_documents 테이블에 `company_doc_no TEXT` 컬럼 추가하면 서버 동기화에도 회사 문서번호 보존 가능
  - SHI-04(화재예방), SHI-05(미끄러짐·넘어짐), SHI-09(고객 폭언·폭행), SHI-10(물류센터 지게차) 4건은 대응하는 회사 문서 미확인 상태
  - SHP-01(조직상황), SHP-20(근로자 건강관리), SHP-21(고객응대근로자 보호) 3건도 회사 문서번호 미매핑
  - 44종 문서의 실제 DOCX 본문은 앱의 "본문" 필드에 입력되지 않았음 — 필요 시 별도 작업

### 2026-09-04 사이트 로딩 오류 수정 + D드라이브 기반 companyStatus 14건 업데이트
- 수정한 파일:
  - `js/views-core.js` — 대시보드 바로가기 template literal 백슬래시 이스케이프 제거 (사이트 로딩 불가 원인)
  - `js/data/frameworks.js` — companyStatus 14건 업데이트 (빈칸/메모→실제 이행현황)
  - `index.html`, `js/app.js`, `js/core.js`, `js/views-ext.js` — 캐시버스트 20260904_iso→fix→doc
- 수정한 기능:
  - views-core.js 406~410행: `\`` → `` ` ``, `\${` → `${` (ES Module strict mode 구문 오류 수정)
  - MSSA-5-4, OSHA-017/025/034/042/051/058/084/114/119/125/128/164/618 companyStatus 채움
  - 데이터 소스: D드라이브 26년 심사 대응 자료 ZIP(5,110파일), 산재 통계(8번 폴더), 실행문서 현황표
- 건드리지 않은 기능:
  - 로그인/가입 로직, Supabase RLS, R2 Worker
  - 기존 완성된 companyStatus (MSSA 9건, OSHA 22건, ISO 30건)
  - DOC_MASTER 64건, 조항 id, evidenceFiles
- 테스트한 항목 / 결과:
  - `node --check` 5개 JS 파일 전체 통과
  - 운영 사이트 로딩 정상 확인 (콘솔 에러 없음)
  - 캐시버스트 `20260904_doc` 반영 확인
- 확인하지 못한 것:
  - 로그인 후 대시보드·법령/ISO 목록 화면에서 업데이트된 companyStatus 표시 육안 미확인
  - 26년 심사 대응 자료 ZIP 내 개별 증빙파일은 evidenceFiles에 미등록 (5,110파일 — 별도 작업 필요)
- 다음 작업자가 봐야 할 것:
  - OSHA-119(석면), 125(작업환경측정), 128(휴게시설), 164(서류보존) — "확인 필요"로 기재, 실제 현황 파악 후 구체화
  - 26년 심사 대응 자료 ZIP(본사·남사·부산·안성 4개 사이트)의 개별 증빙파일을 evidenceFiles에 등록하려면 별도 작업
  - OSHA-054 산재 통계 — 현재 1~7월 기준, 8월 산재승인 데이터 확정 후 갱신 필요

### 2026-09-04 문서체계 상태 마이그레이션 (구축률 0% → 88%)
- 수정한 파일:
  - `js/core.js` — seedDocuments() 기본 상태 로직 개선, migrateDocStatus() 추가
  - `index.html`, `js/app.js`, `js/views-core.js`, `js/views-ext.js` — 캐시버스트 `20260904_docst`
- 수정한 기능:
  - seedDocuments()에서 companyDocNo가 있는 문서 → status:'approved' 기본값, 양식(SHF)도 approved
  - companyDocNo의 `(N)` 패턴에서 개정번호(Rev) 자동 추출
  - migrateDocStatus(): 기존 브라우저 데이터 일회성 업그레이드 (MIG_KEY: shms.doc_migration_v2)
    - DOC_MASTER에 있지만 state.documents에 없는 문서 18건 자동 추가 (SHM-00, SHP-22~23, SHI-13~27)
    - draft 상태 문서 중 companyDocNo 보유분 → approved 승격
    - company_doc_no, version 필드 DOC_MASTER 기준 보정
- 건드리지 않은 기능:
  - DOC_MASTER 조항 id·구조 변경 없음
  - 문서 저장(saveDocument) 로직 변경 없음
  - Supabase 테이블 스키마 변경 없음
- 테스트한 항목 / 결과:
  - 로컬 서버 문서체계 페이지: 전체 64건, 제정/승인 56건, 구축률 88% 확인
  - 초안 8건: SHM-00, SHP-01, SHP-20, SHP-21, SHI-04, SHI-05, SHI-09, SHI-10 (companyDocNo 미부여)
  - 절차서 Rev 번호 자동 표시 확인 (예: SHP-02 Rev.6, SHP-09 Rev.4)
  - 문법검사 전 파일 통과
- 확인하지 못한 것:
  - 운영 사이트(GitHub Pages)에서 마이그레이션 적용 후 실제 문서 수 확인 (Supabase 문서 테이블과 병합 결과)
  - 초안 8건 중 실제 회사 문서번호가 존재하는지 여부 (사용자 확인 필요)
- 다음 작업자가 봐야 할 것:
  - 초안 8건(SHM-00, SHP-01, SHP-20, SHP-21, SHI-04, SHI-05, SHI-09, SHI-10)에 회사 문서번호 부여 시 DOC_MASTER에 companyDocNo 추가 필요
  - 마이그레이션 키 `shms.doc_migration_v2` — 다음 마이그레이션 필요 시 v3으로 올려야 함
  - PDF 보고서 파일(docs/reports/ 폴더)은 아직 미복사 — 수동 복사 필요
